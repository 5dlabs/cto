apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: databases

resources:
  - cluster.yaml

commonLabels:
  app.kubernetes.io/name: redis
  app.kubernetes.io/component: database
  app.kubernetes.io/managed-by: kustomize

commonAnnotations:
  description: "Redis Cluster with Sentinel HA"
