# Test Task: Sidecar Validation

Create a simple test file `/workspace/test-output.txt` that contains "Hello from Claude CLI test" and then list the contents of the workspace directory.

This is a quick validation test to ensure the streaming output is being captured correctly by the sidecar.
