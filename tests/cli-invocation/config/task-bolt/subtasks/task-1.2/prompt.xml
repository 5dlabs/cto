<?xml version="1.0" encoding="UTF-8"?>
<subtask id="1.2" parent="1" agent="bolt">
    <meta>
        <title>Deploy Caching and Message Queue Services (Redis/Valkey and RabbitMQ)</title>
        <dependencies>None</dependencies>
    </meta>

    <role>You are a DevOps Engineer specializing in Kubernetes. Implement subtask 1.2.</role>

    <context>
        <overview>Provision Redis/Valkey cluster for caching and rate limiting capabilities, and RabbitMQ cluster for reliable task queuing and message processing.</overview>
        <parent_task>Setup Infrastructure Components (Bolt - Kubernetes)</parent_task>
    </context>

    <requirements>
        <description>Deploy Valkey/Redis cluster with appropriate memory limits and persistence configuration for caching and rate limiting. Deploy RabbitMQ cluster operator with proper queue configurations, clustering setup, and management interface access. Configure high availability and failover mechanisms for both services.</description>
    </requirements>

    <acceptance_criteria>
        <criterion>Subtask requirements implemented</criterion>
        <criterion>Parent task requirements still satisfied</criterion>
    </acceptance_criteria>

    <validation>
        <command>kubectl apply --dry-run=client -f .</command>
        <command>helm lint charts/*</command>
    </validation>
</subtask>
