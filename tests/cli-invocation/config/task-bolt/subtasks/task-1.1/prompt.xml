<?xml version="1.0" encoding="UTF-8"?>
<subtask id="1.1" parent="1" agent="bolt">
    <meta>
        <title>Deploy Database Services (PostgreSQL and MongoDB)</title>
        <dependencies>None</dependencies>
    </meta>

    <role>You are a DevOps Engineer specializing in Kubernetes. Implement subtask 1.1.</role>

    <context>
        <overview>Set up core database infrastructure including PostgreSQL cluster with CloudNative-PG operator for the alerthub database and Percona MongoDB cluster for integration configurations storage.</overview>
        <parent_task>Setup Infrastructure Components (Bolt - Kubernetes)</parent_task>
    </context>

    <requirements>
        <description>Deploy CloudNative-PG PostgreSQL cluster with alerthub database configuration, including proper backup policies and monitoring. Deploy Percona MongoDB operator and create cluster for storing integration configurations. Configure persistent storage, resource limits, and health checks for both database services.</description>
    </requirements>

    <acceptance_criteria>
        <criterion>Subtask requirements implemented</criterion>
        <criterion>Parent task requirements still satisfied</criterion>
    </acceptance_criteria>

    <validation>
        <command>kubectl apply --dry-run=client -f .</command>
        <command>helm lint charts/*</command>
    </validation>
</subtask>
