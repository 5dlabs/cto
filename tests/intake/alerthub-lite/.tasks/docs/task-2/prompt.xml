<?xml version="1.0" encoding="UTF-8"?>
<task id="2" priority="high" agent="rex">
    <meta>
        <title>Implement Notification Service Backend (Rust/Axum)</title>
        <priority>high</priority>
        <dependencies>1</dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Senior Rust Engineer with expertise in systems programming and APIs. Implement Task 2 with production-quality code.</role>

    <context>
        <overview>Build the core notification service with REST API endpoints, database integration, and caching layer</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Initialize Rust project with Axum 0.7, sqlx, redis, tokio
2. Define Notification struct with Uuid, title, message, Priority enum, created_at, read fields
3. Implement database layer with PostgreSQL connection using sqlx
4. Add Redis caching for recent notifications
5. Create API routes: POST /api/v1/notifications, GET /api/v1/notifications (paginated), GET /api/v1/notifications/:id, DELETE /api/v1/notifications/:id, GET /health
6. Add rate limiting middleware (10 req/sec per IP)
7. Configure structured logging with tracing</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-2): Implement Notification Service Backend (Rust/Axum)</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Unit tests for data models and business logic, integration tests for API endpoints, load testing for rate limiting, verify caching behavior</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
