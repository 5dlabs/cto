<?xml version="1.0" encoding="UTF-8"?>
<task id="4" priority="high" agent="bolt">
    <meta>
        <title>Kafka Event Streaming Setup (Bolt - Kubernetes)</title>
        <priority>high</priority>
        <dependencies>1</dependencies>
        <agent_hint>bolt</agent_hint>
    </meta>

    <role>You are a Senior DevOps Engineer with expertise in Kubernetes, GitOps, and CI/CD. Implement Task 4 with production-quality code.</role>

    <context>
        <overview>Deploy Strimzi operator and Kafka cluster for async event processing</overview>
        <background>
            This task is part of a larger project. Key resources:
            - PRD: .tasks/docs/prd.txt (full requirements and architecture)
            - Related tasks: 1
            - Infrastructure config: Check ConfigMaps for connection strings
        </background>
    </context>

    <requirements>
        <description>1. Install Strimzi operator\n2. Create Kafka cluster with 1 broker\n3. Create topics for notifications.created, notifications.delivered, notifications.failed\n4. Configure topic partitions and retention\n5. Set up Kafka Connect if needed</description>
        <constraints>
            <constraint>Match existing codebase patterns and conventions</constraint>
            <constraint>Create PR with atomic, well-described commits</constraint>
            <constraint>Include unit tests for new functionality</constraint>
            <constraint>PR title format: feat(task-4): Kafka Event Streaming Setup (Bolt - Kubernetes)</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements from task description implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions and style guide</criterion>
        <criterion>Kafka cluster is running, topics exist with correct partitions, can produce/consume messages via kafka-console tools</criterion>
    </acceptance_criteria>

    <validation>
        <command>kubectl get pods -n {namespace} - verify all pods running</command>
        <command>kubectl get secrets - verify connection secrets created</command>
        <command>kubectl logs {pod} - check for errors</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation of described functionality</deliverable>
        <deliverable>Unit tests covering new code paths</deliverable>
        <deliverable>Pull request with clear description</deliverable>
    </deliverables>
</task>
