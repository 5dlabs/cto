<?xml version="1.0" encoding="UTF-8"?>
<task id="13" priority="medium" agent="spark">
    <meta>
        <title>Build and Distribute Desktop Client (Spark - Electron)</title>
        <priority>medium</priority>
        <dependencies>7, 8, 9, 10</dependencies>
        <agent_hint>spark</agent_hint>
    </meta>

    <role>You are a Senior Desktop Engineer with expertise in Electron and native integrations. Implement Task 13 with production-quality code.</role>

    <context>
        <overview>Build production installers for Windows, macOS, and Linux and setup auto-update</overview>
        <background>
            This task is part of a larger project. Key resources:
            - PRD: .tasks/docs/prd.txt (full requirements and architecture)
            - Related tasks: 7, 8, 9, 10
            - Infrastructure config: Check ConfigMaps for connection strings
        </background>
    </context>

    <requirements>
        <description>1. Configure electron-builder in package.json:
   &quot;build&quot;: {
     &quot;appId&quot;: &quot;com.alerthub.desktop&quot;,
     &quot;productName&quot;: &quot;AlertHub&quot;,
     &quot;directories&quot;: { &quot;output&quot;: &quot;dist&quot; },
     &quot;files&quot;: [&quot;build/**/*&quot;, &quot;node_modules/**/*&quot;, &quot;package.json&quot;],
     &quot;mac&quot;: { &quot;category&quot;: &quot;public.app-category.productivity&quot;, &quot;target&quot;: [&quot;dmg&quot;, &quot;zip&quot;] },
     &quot;win&quot;: { &quot;target&quot;: [&quot;nsis&quot;, &quot;portable&quot;] },
     &quot;linux&quot;: { &quot;target&quot;: [&quot;AppImage&quot;, &quot;deb&quot;] }
   }

2. Setup code signing:
   - macOS: Obtain Apple Developer certificate, configure CSC_LINK and CSC_KEY_PASSWORD env vars
   - Windows: Obtain code signing certificate, configure CSC_LINK and CSC_KEY_PASSWORD env vars

3. Build for all platforms:
   - macOS: npm run dist -- --mac
   - Windows: npm run dist -- --win
   - Linux: npm run dist -- --linux

4. Setup auto-update with electron-updater:
   - Configure update server URL in main process
   - Add update check on app startup
   - Implement update download and install flow
   - Test update with mock server

5. Create distribution packages:
   - macOS: DMG installer with drag-and-drop
   - Windows: NSIS installer with auto-update support
   - Linux: AppImage and Deb package

6. Setup GitHub Releases for distribution:
   - Create GitHub release with version tag
   - Upload installers to release assets
   - Configure electron-updater to check GitHub releases

7. Create download page:
   - Add download links to web console
   - Detect user OS and suggest appropriate installer
   - Provide checksums for verification

8. Test installers:
   - Install on clean Windows 10/11 VM
   - Install on macOS 12+ (Intel and Apple Silicon)
   - Install on Ubuntu 22.04 LTS
   - Verify app launches and functions correctly
   - Test auto-update flow</description>
        <constraints>
            <constraint>Match existing codebase patterns and conventions</constraint>
            <constraint>Create PR with atomic, well-described commits</constraint>
            <constraint>Include unit tests for new functionality</constraint>
            <constraint>PR title format: feat(task-13): Build and Distribute Desktop Client (Spark - Electron)</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements from task description implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions and style guide</criterion>
        <criterion>1. Test installers on all target platforms
2. Verify code signing (no security warnings)
3. Test auto-update from previous version
4. Test system tray integration
5. Test global shortcuts
6. Test native notifications
7. Verify WebSocket connection
8. Test multi-window behavior
9. Verify auto-launch on boot
10. Check app size (target: &lt;100MB)</criterion>
    </acceptance_criteria>

    <validation>
        <command>npm run build</command>
        <command>npm run lint</command>
        <command>npm run test</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation of described functionality</deliverable>
        <deliverable>Unit tests covering new code paths</deliverable>
        <deliverable>Pull request with clear description</deliverable>
    </deliverables>
</task>
