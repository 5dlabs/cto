<?xml version="1.0" encoding="UTF-8"?>
<task id="10" priority="high" agent="grizz">
    <meta>
        <title>Deploy Admin API Service (Grizz - Go/gRPC)</title>
        <priority>high</priority>
        <dependencies>4</dependencies>
        <agent_hint>grizz</agent_hint>
    </meta>

    <role>You are a Senior Go Engineer with expertise in concurrent systems and microservices. Implement Task 10 with production-quality code.</role>

    <context>
        <overview>Create Kubernetes manifests and deploy the Admin API service to the cluster</overview>
        <background>
            This task is part of a larger project. Key resources:
            - PRD: .tasks/docs/prd.txt (full requirements and architecture)
            - Related tasks: 4
            - Infrastructure config: Check ConfigMaps for connection strings
        </background>
    </context>

    <requirements>
        <description>1. Create Kubernetes manifests in k8s/admin-api/:
   - deployment.yaml: Deployment with 2 replicas, resource limits (256Mi-512Mi memory, 200m-400m CPU), health checks (liveness: /health, readiness: /health/ready), env vars from ConfigMaps and Secrets
   - service.yaml: ClusterIP service on port 9090 (gRPC), 8080 (REST gateway)
   - hpa.yaml: HorizontalPodAutoscaler targeting 70% CPU, min 2, max 6 replicas
   - configmap.yaml: ConfigMap with POSTGRES_URL, REDIS_URL
   - secret.yaml: Secret with JWT_PRIVATE_KEY, POSTGRES_PASSWORD

2. Create Dockerfile in admin-api/:
   FROM golang:1.22 AS builder
   WORKDIR /app
   COPY go.mod go.sum ./
   RUN go mod download
   COPY . .
   RUN CGO_ENABLED=0 GOOS=linux go build -o admin-api cmd/server/main.go
   FROM alpine:latest
   RUN apk --no-cache add ca-certificates
   COPY --from=builder /app/admin-api /usr/local/bin/
   EXPOSE 9090 8080
   CMD [&quot;admin-api&quot;]

3. Build and push Docker image:
   docker build -t alerthub/admin-api:latest .
   docker push alerthub/admin-api:latest

4. Apply Kubernetes manifests:
   kubectl apply -f k8s/admin-api/

5. Verify deployment:
   kubectl get pods -l app=admin-api
   kubectl logs -l app=admin-api --tail=100
   kubectl port-forward svc/admin-api 9090:9090 8080:8080
   grpcurl -plaintext localhost:9090 list
   curl http://localhost:8080/api/v1/tenants

6. Configure Ingress:
   - Create Ingress resource for /api/v1/tenants, /api/v1/users, /api/v1/rules, /api/v1/analytics paths
   - Configure TLS with cert-manager</description>
        <constraints>
            <constraint>Match existing codebase patterns and conventions</constraint>
            <constraint>Create PR with atomic, well-described commits</constraint>
            <constraint>Include unit tests for new functionality</constraint>
            <constraint>PR title format: feat(task-10): Deploy Admin API Service (Grizz - Go/gRPC)</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements from task description implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions and style guide</criterion>
        <criterion>1. Verify pods are running and ready
2. Test gRPC endpoints with grpcurl
3. Test REST endpoints via grpc-gateway
4. Test JWT authentication with valid/invalid tokens
5. Test RBAC with different roles
6. Test tenant CRUD operations
7. Test user CRUD operations
8. Test rule CRUD operations
9. Test analytics endpoints
10. Verify audit logging</criterion>
    </acceptance_criteria>

    <validation>
        <command>go test ./...</command>
        <command>golangci-lint run</command>
        <command>go build ./...</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation of described functionality</deliverable>
        <deliverable>Unit tests covering new code paths</deliverable>
        <deliverable>Pull request with clear description</deliverable>
    </deliverables>
</task>
