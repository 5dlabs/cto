<?xml version="1.0" encoding="UTF-8"?>
<task id="14" priority="high" agent="bolt">
    <meta>
        <title>Setup Monitoring and Observability</title>
        <priority>high</priority>
        <dependencies>8, 9, 10</dependencies>
        <agent_hint>bolt</agent_hint>
    </meta>

    <role>You are a Senior DevOps Engineer with expertise in Kubernetes, GitOps, and CI/CD. Implement Task 14 with production-quality code.</role>

    <context>
        <overview>Deploy Prometheus, Grafana, and configure dashboards for monitoring AlertHub services</overview>
        <background>
            This task is part of a larger project. Key resources:
            - PRD: .tasks/docs/prd.txt (full requirements and architecture)
            - Related tasks: 8, 9, 10
            - Infrastructure config: Check ConfigMaps for connection strings
        </background>
    </context>

    <requirements>
        <description>1. Deploy Prometheus using kube-prometheus-stack:
   helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
   helm install prometheus prometheus-community/kube-prometheus-stack -n monitoring --create-namespace

2. Configure ServiceMonitors for AlertHub services:
   - Create ServiceMonitor for notification-router (scrape /metrics on port 8080)
   - Create ServiceMonitor for integration-service (scrape /metrics on port 3000)
   - Create ServiceMonitor for admin-api (scrape /metrics on port 8080)

3. Create Grafana dashboards:
   - Dashboard 1: Service Health Overview
     * Panels: Service uptime, request rate, error rate, latency (p50, p95, p99)
     * Filters: Service, time range
   - Dashboard 2: Notification Metrics
     * Panels: Notifications submitted, delivered, failed, delivery time, channel breakdown
   - Dashboard 3: Infrastructure Metrics
     * Panels: PostgreSQL connections, Redis memory, Kafka lag, RabbitMQ queue depth
   - Dashboard 4: Resource Usage
     * Panels: CPU usage, memory usage, network I/O per service

4. Configure Prometheus alerts:
   - HighErrorRate: Alert when error rate &gt; 5% for 5 minutes
   - HighLatency: Alert when p95 latency &gt; 500ms for 5 minutes
   - ServiceDown: Alert when service is down for 1 minute
   - HighQueueDepth: Alert when RabbitMQ queue depth &gt; 10000
   - DatabaseConnectionPoolExhausted: Alert when PostgreSQL connections &gt; 90%

5. Setup Alertmanager:
   - Configure Slack integration for alerts
   - Configure email integration for critical alerts
   - Set up alert routing (critical -&gt; Slack + email, warning -&gt; Slack)

6. Configure log aggregation:
   - Deploy Loki for log aggregation
   - Configure Promtail to scrape logs from all pods
   - Add Loki data source to Grafana
   - Create log dashboard with filters (service, level, trace ID)

7. Setup distributed tracing (optional):
   - Deploy Jaeger or Tempo
   - Configure OpenTelemetry in services
   - Add tracing data source to Grafana
   - Create trace dashboard</description>
        <constraints>
            <constraint>Match existing codebase patterns and conventions</constraint>
            <constraint>Create PR with atomic, well-described commits</constraint>
            <constraint>Include unit tests for new functionality</constraint>
            <constraint>PR title format: feat(task-14): Setup Monitoring and Observability</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements from task description implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions and style guide</criterion>
        <criterion>1. Verify Prometheus is scraping metrics from all services
2. Verify Grafana dashboards display data correctly
3. Test alert rules by triggering conditions (e.g., stop a service)
4. Verify alerts are sent to Slack and email
5. Test log aggregation (search for specific log entries)
6. Verify trace data is collected (if enabled)
7. Test dashboard filters and time range selection
8. Verify metrics are retained for configured period (default 15 days)</criterion>
    </acceptance_criteria>

    <validation>
        <command>kubectl get pods -n {namespace} - verify all pods running</command>
        <command>kubectl get secrets - verify connection secrets created</command>
        <command>kubectl logs {pod} - check for errors</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation of described functionality</deliverable>
        <deliverable>Unit tests covering new code paths</deliverable>
        <deliverable>Pull request with clear description</deliverable>
    </deliverables>
</task>
