<?xml version="1.0" encoding="UTF-8"?>
<task id="4" priority="high" agent="rex">
    <meta>
        <title>Implement task board with CRUD operations and filtering</title>
        <priority>high</priority>
        <dependencies>3</dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Senior Rust Engineer with expertise in systems programming and APIs. Implement Task 4 with production-quality code.</role>

    <context>
        <overview>Build comprehensive task management system with status tracking, assignments, filtering, and soft delete with 30-day retention</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Create tasks table with soft delete (deleted_at column)
2. Implement task CRUD with validation for title, description, assignee, status, due_date
3. Add status enum (todo, in_progress, done) with database constraints
4. Build filtering by status, assignee, due date range using query parameters
5. Implement soft delete with background cleanup job for 30-day retention
6. Add task assignment validation (assignee must be team member)

```rust
#[derive(sqlx::Type, Serialize, Deserialize)]
#[sqlx(type_name = &quot;task_status&quot;, rename_all = &quot;snake_case&quot;)]
enum TaskStatus { Todo, InProgress, Done }

#[derive(sqlx::FromRow, Serialize)]
struct Task {
    id: Uuid,
    team_id: Uuid,
    title: String,
    description: Option&lt;String&gt;,
    assignee_id: Option&lt;Uuid&gt;,
    status: TaskStatus,
    due_date: Option&lt;DateTime&lt;Utc&gt;&gt;,
    deleted_at: Option&lt;DateTime&lt;Utc&gt;&gt;,
}

// GET /api/teams/:team_id/tasks with filtering
struct TaskFilters {
    status: Option&lt;TaskStatus&gt;,
    assignee_id: Option&lt;Uuid&gt;,
    due_after: Option&lt;DateTime&lt;Utc&gt;&gt;,
    due_before: Option&lt;DateTime&lt;Utc&gt;&gt;,
}
```</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-4): Implement task board with CRUD operations and filtering</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>CRUD operation tests, filtering functionality validation, soft delete behavior verification, task assignment permission tests, 30-day cleanup job testing</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
