<?xml version="1.0" encoding="UTF-8"?>
<task id="6" priority="medium" agent="bolt">
    <meta>
        <title>Create React dashboard with Kanban board and deployment configuration</title>
        <priority>medium</priority>
        <dependencies>5</dependencies>
        <agent_hint>bolt</agent_hint>
    </meta>

    <role>You are a Senior DevOps Engineer with expertise in Kubernetes, GitOps, and CI/CD. Implement Task 6 with production-quality code.</role>

    <context>
        <overview>Build responsive React dashboard with drag-and-drop Kanban board, team management UI, and complete Kubernetes deployment setup with observability</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Setup React 18 + TypeScript + Tailwind CSS project structure
2. Implement Kanban board with react-beautiful-dnd for drag-and-drop
3. Build team activity feed component with real-time WebSocket integration
4. Create member management UI with role assignment
5. Add dark/light theme support using Tailwind CSS
6. Setup Kubernetes manifests with HPA for auto-scaling
7. Configure Prometheus metrics endpoint (/metrics) with custom business metrics
8. Ensure mobile responsiveness for all components

```typescript
// Kanban board component
interface Task {
  id: string;
  title: string;
  status: &apos;todo&apos; | &apos;in_progress&apos; | &apos;done&apos;;
  assignee?: User;
  due_date?: string;
}

const KanbanBoard: React.FC = () =&gt; {
  const [tasks, setTasks] = useState&lt;Task[]&gt;([]);
  
  const onDragEnd = (result: DropResult) =&gt; {
    // Update task status via API
  };
};

// Kubernetes deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: teamsync-api
spec:
  replicas: 3
  template:
    spec:
      containers:
      - name: api
        image: teamsync:latest
        resources:
          requests: {cpu: 100m, memory: 128Mi}
          limits: {cpu: 500m, memory: 512Mi}
```</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-6): Create React dashboard with Kanban board and deployment configuration</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>React component unit tests with Jest/RTL, drag-and-drop functionality tests, WebSocket integration tests, mobile responsiveness validation, Kubernetes deployment verification, Prometheus metrics endpoint testing</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
