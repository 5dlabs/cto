<?xml version="1.0" encoding="UTF-8"?>
<task id="2" priority="high" agent="rex">
    <meta>
        <title>Notification Router Service (Rex - Rust/Axum)</title>
        <priority>high</priority>
        <dependencies>1</dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Senior Rust Engineer with expertise in systems programming and APIs. Implement Task 2 with production-quality code.</role>

    <context>
        <overview>Build the high-performance notification routing service with rate limiting, deduplication, and Kafka integration</overview>
        <background>
            This task is part of a larger project. Key resources:
            - PRD: .tasks/docs/prd.txt (full requirements and architecture)
            - Related tasks: 1
            - Infrastructure config: Check ConfigMaps for connection strings
        </background>
    </context>

    <requirements>
        <description>Create Rust/Axum 0.7 service with endpoints: POST /api/v1/notifications, batch endpoint, GET status, WebSocket for real-time, health checks, and Prometheus metrics. Implement rate limiting via Redis, priority queues, deduplication with TTL, and dead letter queue.</description>
        <constraints>
            <constraint>Match existing codebase patterns and conventions</constraint>
            <constraint>Create PR with atomic, well-described commits</constraint>
            <constraint>Include unit tests for new functionality</constraint>
            <constraint>PR title format: feat(task-2): Notification Router Service (Rex - Rust/Axum)</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements from task description implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions and style guide</criterion>
        <criterion>Unit tests, integration tests with testcontainers, load test for 10k notifications/minute</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test --all</command>
        <command>cargo clippy -- -D warnings</command>
        <command>cargo fmt --check</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation of described functionality</deliverable>
        <deliverable>Unit tests covering new code paths</deliverable>
        <deliverable>Pull request with clear description</deliverable>
    </deliverables>
</task>
