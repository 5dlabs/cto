---
id: "2014277127244296688"
author: "@mattpocockuk"
posted_at: "2026-01-22T10:01:05.251+00:00"
processed_at: "2026-01-24T10:07:38.045177564+00:00"
implementation_score: 0.82
implementation_priority: "🔍 Worth Investigating"
implementation_ideas:
  - "Build a Rust-based streaming output handler for Argo Workflows that maintains real-time logs during batch agent execution"
  - "Create a terminal multiplexer integration for monitoring multiple agents (Rex, Blaze, Tess) simultaneously with live streaming"
  - "Implement a 'watch mode' CLI tool for developers to stream agent outputs even when workflows run in background"
  - "Add streaming WebSocket endpoints to our platform API for real-time agent output consumption"
  - "Develop a tmux/screen-like session manager specifically for AI agent orchestration with persistent streaming"
  - "Create a logging middleware in Rust that buffers and streams agent outputs to multiple consumers (terminal, web UI, logs)"
topics:
  - ai_agents
  - streaming_output
  - terminal_multiplexing
  - background_processes
  - developer_experience
  - bash_scripting
  - agent_execution
tags:
  - ai_agents
  - streaming_output
  - terminal_multiplexing
  - background_processes
  - developer_experience
  - bash_scripting
  - agent_execution
---

# Research: ai_agents

## Original Tweet

> AFK Ralph in a bash loop is SO much better than the Claude Code plugin.
> 
> But the DX is a bit nasty. Running it AFK means no streaming to the terminal by default.
> 
> This script fixes it:
> 
> https://aihero.dev/s/lvmQGl

**Author**: @mattpocockuk (mattpocockuk)  
**Posted**: January 22, 2026 at 10:01 AM

## Analysis

This tweet references a practical DX improvement for running AI coding agents in 'AFK' (away from keyboard) mode with streaming output. The linked script addresses a real pain point: maintaining terminal streaming when running automated coding agents in background/batch mode. This is HIGHLY relevant to our platform since we orchestrate multiple AI agents (Rex, Blaze, Tess) that need to run autonomously while providing real-time feedback. The comparison to 'Claude Code plugin' suggests this is about agent execution patterns. We could implement: (1) Streaming output handlers for our Argo Workflows that maintain real-time logs even in batch mode, (2) Terminal multiplexing patterns for multi-agent orchestration, (3) Better DX tooling for developers monitoring agent execution. The bash script approach suggests lightweight, practical solutions we could adapt to our Rust-based agent runners. This is actionable because it addresses a specific technical problem (streaming in background processes) with a concrete solution pattern.

**Implementation Potential**: 🔍 Worth Investigating (0.82)

### 💡 Implementation Ideas

- Build a Rust-based streaming output handler for Argo Workflows that maintains real-time logs during batch agent execution
- Create a terminal multiplexer integration for monitoring multiple agents (Rex, Blaze, Tess) simultaneously with live streaming
- Implement a 'watch mode' CLI tool for developers to stream agent outputs even when workflows run in background
- Add streaming WebSocket endpoints to our platform API for real-time agent output consumption
- Develop a tmux/screen-like session manager specifically for AI agent orchestration with persistent streaming
- Create a logging middleware in Rust that buffers and streams agent outputs to multiple consumers (terminal, web UI, logs)

---

## Supporting Content

### Here's How To Stream Claude Code With AFK Ralph

**URL**: <https://aihero.dev/s/lvmQGl>

![](<Base64-Image-Removed>)

[← All Posts](https://www.aihero.dev/posts)

# Here's How To Stream Claude Code With AFK Ralph

![Matt Pocock](https://res.cloudinary.com/total-typescript/image/upload/c_limit,w_96/f_auto/q_auto/v1728059672/matt-pocock_eyjjli?_a=BAVAZGAQ0)

Matt Pocock

Use with AI

This guide is for people who've tried Ralph with Claude Code and hit that frustration point: running the AFK script and ending up staring at a blank screen.

If you've never heard of Ralph, start here:

- [Getting Started With Ralph](https://www.aihero.dev/getting-started-with-ralph/)
- [11 Tips For AI Coding With Ralph](https://www.aihero.dev/tips-for-ai-coding-with-ralph-wiggum)

## [The Problem: A Blank Screen](https://www.aihero.dev/heres-how-to-stream-claude-code-with-afk-ralph\#the-problem-a-blank-screen)

When you want Ralph to run while you're away from your keyboard, you might use a script like this:

```
#!/bin/bash

set -e

if [ -z "$1" ]; then

  echo "Usage: $0 <iterations>"

  exit 1

fi

for ((i=1; i<=$1; i++)); do

  result=$(docker sandbox run --credentials host claude \

    --print \

    "<your prompt here>")

  if [[ "$result" == *"<promise>COMPLETE</promise>"* ]]; then

    echo "Ralph complete after $i iterations."

    exit 0

  fi

done
```

The issue here is frustrating: when you run Claude with the `--print` flag, you get zero streaming output. Your terminal goes blank.

You walk away, and you have absolutely no idea what's happening. Is Claude working? Is it stuck? Did something break? You won't know until it's finished.

The dream with AFK Ralph is to get the best of both worlds: you want real-time visibility into what Claude is doing, but you also want to leave it running while you step away.

## [The Solution: Streaming with jq](https://www.aihero.dev/heres-how-to-stream-claude-code-with-afk-ralph\#the-solution-streaming-with-jq)

Claude can output `stream-json` format, which gives you every single message as it happens. But that output is extremely verbose and unreadable.

By combining `stream-json` with `jq` filtering, you can extract just the useful information and stream it to your terminal in real-time. At the same time, you capture the final result to check for the `<promise>COMPLETE</promise>` marker.

Here's the complete script:

```
#!/bin/bash

set -e

if [ -z "$1" ]; then

  echo "Usage: $0 <iterations>"

  exit 1

fi

# jq filter to extract streaming text from assistant messages

stream_text='select(.type == "assistant").message.content[]? | select(.type == "text").text // empty | gsub("\n"; "\r\n") | . + "\r\n\n"'

# jq filter to extract final result

final_result='select(.type == "result").result // empty'

for ((i=1; i<=$1; i++)); do

  tmpfile=$(mktemp)

  trap "rm -f $tmpfile" EXIT

  docker sandbox run --credentials host claude \

    --verbose \

    --print \

    --output-format stream-json \

    "<your prompt here>" \

  | grep --line-buffered '^{' \

  | tee "$tmpfile" \

  | jq --unbuffered -rj "$stream_text"

  result=$(jq -r "$final_result" "$tmpfile")

  if [[ "$result" == *"<promise>COMPLETE</promise>"* ]]; then

    echo "Ralph complete after $i iterations."

    exit 0

  fi

done
```

This script accepts one argument: the number of iterations to run.

## [Walking Through the Script Structure](https://www.aihero.dev/heres-how-to-stream-claude-code-with-afk-ralph\#walking-through-the-script-structure)

### [Breaking Down the Stream Filter](https://www.aihero.dev/heres-how-to-stream-claude-code-with-afk-ralph\#breaking-down-the-stream-filter)

The stream filter does several important things:

1. **Selects assistant messages**: `select(.type == "assistant")` grabs only Claude's responses
2. **Extracts text content**: `.message.content[]? | select(.type == "text").text` pulls out just the text portions
3. **Fixes line endings**: `gsub("\n"; "\r\n")` replaces newlines with carriage return + newline
4. **Adds spacing**: `. + "\r\n\n"` inserts extra space between messages

The carriage return replacement fixes a bug where the cursor wasn't returning to the first character of the line properly.

### [The Data Pipeline](https://www.aihero.dev/heres-how-to-stream-claude-code-with-afk-ralph\#the-data-pipeline)

Here's how data flows through the script:

Docker streams out `stream-json` formatted data, but it includes some non-JSON lines just for noise. The `grep --line-buffered '^{'` filter ensures only valid JSON lines get processed.

The `tee "$tmpfile"` command writes everything to a temporary file without stopping the stream. You need this file later to check if Claude has finished.

Finally, `jq --unbuffered -rj "$stream_text"` applies the streaming filter and displays the text in real-time to your terminal.

## [Conclusion](https://www.aihero.dev/heres-how-to-stream-claude-code-with-afk-ralph\#conclusion)

My hope is that relatively soon I'll be able to delete this article because Claude Code will have shipped a feature that allows you to stream the responses while still capturing the final output.

OpenCode already has this, and so there's no need to write an article like this for OpenCode. But until then, this is a workable solution to get real-time streaming output from Claude while running AFK Ralph.

![](https://res.cloudinary.com/total-typescript/image/upload/c_limit,w_384/f_auto/q_auto/v1741008166/aihero.dev/assets/textured-logo-mark_2x_ecauns?_a=BAVAZGAQ0)

## Join over 50,000 Developers Becoming AI Heroes

### Subscribe to be the first to learn about AI Hero releases, updates, and special discounts for AI Heroes.

First Name

Email\*

Subscribe

I respect your privacy. Unsubscribe at any time.

**Share**

Copy URL

---

## References

- **Original Tweet**: <https://x.com/mattpocockuk/status/2014277127244296688>
- **Here's How To Stream Claude Code With AFK Ralph**: <https://aihero.dev/s/lvmQGl>

---

*Curated by CTO Research Pipeline*
