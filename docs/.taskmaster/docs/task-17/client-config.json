{
  "remoteTools": [
    {
      "name": "kubernetes",
      "description": "Kubernetes cluster management for Argo Workflows",
      "capabilities": ["apply", "get", "describe", "logs", "delete"]
    },
    {
      "name": "argo-cli",
      "description": "Argo Workflows CLI for workflow management", 
      "capabilities": ["submit", "get", "logs", "delete", "retry"]
    },
    {
      "name": "redis-cli",
      "description": "Redis client for state storage operations",
      "capabilities": ["get", "set", "del", "keys", "monitor"]
    },
    {
      "name": "rust-analyzer",
      "description": "Rust language server for controller development",
      "capabilities": ["syntax-check", "type-check", "completion"]
    },
    {
      "name": "cargo",
      "description": "Rust build tool for testing and development", 
      "capabilities": ["build", "test", "check", "bench"]
    },
    {
      "name": "postgresql-client",
      "description": "PostgreSQL client for database state storage",
      "capabilities": ["query", "schema", "backup", "restore"]
    }
  ],
  "localServers": {
    "workflow-dev": {
      "command": "cargo",
      "args": ["watch", "-x", "check", "-x", "test"],
      "cwd": "controller/",
      "env": {
        "RUST_LOG": "debug",
        "DATABASE_URL": "postgresql://localhost/taskmaster"
      }
    },
    "redis-local": {
      "command": "redis-server",
      "args": ["--port", "6380", "--save", ""],
      "cwd": "./",
      "env": {}
    },
    "workflow-validator": {
      "command": "argo",
      "args": ["lint", "workflows/multi-task-processing.yaml"],
      "cwd": "./",
      "env": {
        "KUBECONFIG": "./test-kubeconfig"
      }
    }
  }
}