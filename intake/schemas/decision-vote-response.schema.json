{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DecisionVoteResponse",
  "description": "A committee member's vote on a deliberation decision point",
  "type": "object",
  "required": ["chosen_option", "confidence", "reasoning", "concerns"],
  "additionalProperties": false,
  "properties": {
    "chosen_option": {
      "type": "string",
      "description": "The option chosen by this voter (must match one of the presented options)"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Confidence in the choice (0 = uncertain/abstain, 1 = very confident)"
    },
    "reasoning": {
      "type": "string",
      "description": "Brief explanation of why this option was chosen"
    },
    "concerns": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Concerns about the chosen approach or risks to monitor"
    }
  }
}
