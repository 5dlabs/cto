<?xml version="1.0" encoding="UTF-8"?>
<task id="6" priority="medium" agent="tap">
    <meta>
        <title>Develop Mobile Application (Tap - Expo/React Native)</title>
        <priority>medium</priority>
        <dependencies>task-2, task-3, task-4</dependencies>
        <agent_hint>tap</agent_hint>
    </meta>

    <role>You are a Mobile Engineer specializing in React Native and Expo. Implement Task 6 with production-quality code.</role>

    <context>
        <overview>Build the mobile app using Expo SDK 50+ and React Native with push notification support, offline caching, and biometric authentication for iOS and Android.</overview>
        <background>
            This task is part of AlertHub notification platform.
            - PRD: .tasks/docs/prd.md
            - Related tasks: task-2, task-3, task-4
        </background>
    </context>

    <requirements>
        <description>1. Initialize Expo project with React Native and NativeWind
2. Set up navigation with bottom tabs and stack navigation
3. Implement push notification registration (FCM/APNs)
4. Build notification feed screen with pull-to-refresh
5. Create notification detail screen with actions
6. Add biometric authentication (Face ID, fingerprint)
7. Implement offline notification caching with AsyncStorage
8. Build integrations and settings screens
9. Add deep linking for notification details
10. Configure app badge count and background processing</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-6): Develop Mobile Application (Tap - Expo/React Native)</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>App builds for both iOS and Android, push notifications are received and displayed, biometric authentication works, offline cached notifications are available, navigation flows correctly, deep links open specific notifications, and app badge shows unread count.</criterion>
    </acceptance_criteria>

    <decision_points>
        <decision_point id="d11" category="ux-behavior" constraint="soft" requires_approval="true">
            <description>How should push notifications be grouped when multiple arrive quickly?</description>
            <options><option>individual-notifications</option><option>grouped-by-channel</option><option>summary-notification</option></options>
        </decision_point>
        <decision_point id="d12" category="performance" constraint="soft" requires_approval="false">
            <description>How many notifications should be cached offline?</description>
            <options><option>last-100</option><option>last-500</option><option>configurable-limit</option></options>
        </decision_point>
    </decision_points>

    <validation>
        <command>npx expo-doctor</command>
        <command>npm run lint</command>
        <command>npm run test</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
