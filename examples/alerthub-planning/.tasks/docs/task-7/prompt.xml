<?xml version="1.0" encoding="UTF-8"?>
<task id="7" priority="medium" agent="spark">
    <meta>
        <title>Build Desktop Client (Spark - Electron)</title>
        <priority>medium</priority>
        <dependencies>task-2, task-3, task-4</dependencies>
        <agent_hint>spark</agent_hint>
    </meta>

    <role>You are a Desktop Engineer specializing in Electron. Implement Task 7 with production-quality code.</role>

    <context>
        <overview>Create the desktop system tray application using Electron 28+ with React for native desktop notifications, quick actions, and cross-platform support (Windows, macOS, Linux).</overview>
        <background>
            This task is part of AlertHub notification platform.
            - PRD: .tasks/docs/prd.md
            - Related tasks: task-2, task-3, task-4
        </background>
    </context>

    <requirements>
        <description>1. Initialize Electron project with React and TailwindCSS
2. Set up system tray with notification count badge
3. Implement native desktop notification system
4. Build main window with full notification feed
5. Create mini popup window for quick view
6. Add tray context menu with recent notifications
7. Implement keyboard shortcuts for common actions
8. Add settings window for preferences
9. Configure auto-start on system boot
10. Package for Windows, macOS, and Linux distribution</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-7): Build Desktop Client (Spark - Electron)</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Application runs on all target platforms, system tray icon appears with correct badge count, native notifications display properly, main and mini windows function correctly, keyboard shortcuts work, tray menu shows recent notifications, and auto-start configuration persists.</criterion>
    </acceptance_criteria>

    <decision_points>
        <decision_point id="d13" category="ux-behavior" constraint="soft" requires_approval="false">
            <description>How should the mini popup window be triggered?</description>
            <options><option>hover-tray-icon</option><option>click-tray-icon</option><option>keyboard-shortcut</option><option>configurable</option></options>
        </decision_point>
        <decision_point id="d14" category="security" constraint="hard" requires_approval="true">
            <description>Should desktop client store authentication tokens securely in system keychain?</description>
            <options><option>system-keychain</option><option>encrypted-local-storage</option><option>session-only</option></options>
        </decision_point>
    </decision_points>

    <validation>
        <command>npm run build</command>
        <command>npm run lint</command>
        <command>npm run test</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
