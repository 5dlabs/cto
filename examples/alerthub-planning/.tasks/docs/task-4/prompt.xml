<?xml version="1.0" encoding="UTF-8"?>
<task id="4" priority="high" agent="grizz">
    <meta>
        <title>Admin API gRPC Service (Grizz - Go/gRPC)</title>
        <priority>high</priority>
        <dependencies>task-1</dependencies>
        <agent_hint>grizz</agent_hint>
    </meta>

    <role>You are a Go Engineer specializing in APIs and backend services. Implement Task 4 with production-quality code.</role>

    <context>
        <overview>Implement the management API for tenants, users, rules, and analytics using Go gRPC with grpc-gateway for REST compatibility.</overview>
        <background>
            This task is part of AlertHub notification platform. Key resources:
            - PRD: .tasks/docs/prd.md (full requirements and architecture)
            - Related tasks: task-1
        </background>
    </context>

    <requirements>
        <description>Generate Go code from protobuf definitions for TenantService, UserService, RuleService, and AnalyticsService. Implement JWT authentication, RBAC, PostgreSQL integration, Redis caching, and audit logging. Add grpc-gateway for REST API compatibility.</description>
        <constraints>
            <constraint>Match existing codebase patterns and conventions</constraint>
            <constraint>Create PR with atomic, well-described commits</constraint>
            <constraint>Include unit tests for new functionality</constraint>
            <constraint>PR title format: feat(task-4): Admin API gRPC Service (Grizz - Go/gRPC)</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements from task description implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions and style guide</criterion>
        <criterion>All gRPC services respond correctly, REST endpoints work via grpc-gateway, JWT authentication validates tokens, RBAC enforces permissions, CRUD operations persist to PostgreSQL, and audit logs are created for sensitive operations</criterion>
    </acceptance_criteria>

    <decision_points>
        <decision_point id="d7" category="security" constraint="soft" requires_approval="false">
            <description>JWT token expiration and refresh strategy</description>
            <options><option>15 minute access tokens with refresh tokens</option><option>1 hour access tokens with sliding refresh</option><option>configurable token lifetime per tenant</option></options>
        </decision_point>
        <decision_point id="d8" category="data-model" constraint="open" requires_approval="false">
            <description>Analytics data aggregation frequency</description>
            <options><option>real-time aggregation</option><option>hourly batch aggregation</option><option>daily aggregation with real-time approximations</option></options>
        </decision_point>
    </decision_points>

    <validation>
        <command>go test ./...</command>
        <command>golangci-lint run</command>
        <command>go build ./...</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation of described functionality</deliverable>
        <deliverable>Unit tests covering new code paths</deliverable>
        <deliverable>Pull request with clear description</deliverable>
    </deliverables>
</task>
