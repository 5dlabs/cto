<?xml version="1.0" encoding="UTF-8"?>
<task id="8" priority="high" agent="bolt">
    <meta>
        <title>Configure Kubernetes Deployments (Bolt - Kubernetes)</title>
        <priority>high</priority>
        <dependencies>task-1, task-2, task-3, task-4</dependencies>
        <agent_hint>bolt</agent_hint>
    </meta>

    <role>You are a DevOps Engineer specializing in Kubernetes. Implement Task 8 with production-quality code.</role>

    <context>
        <overview>Create Kubernetes deployment manifests for all services with proper scaling, health checks, secrets management, and network policies. Set up ingress and service mesh for external access.</overview>
        <background>
            This task is part of AlertHub notification platform.
            - PRD: .tasks/docs/prd.md
            - Related tasks: task-1, task-2, task-3, task-4
        </background>
    </context>

    <requirements>
        <description>1. Create deployment manifests for all backend services
2. Set up HPA (Horizontal Pod Autoscaler) for each service
3. Configure service discovery with ClusterIP services
4. Create ingress controller for external traffic routing
5. Set up secrets management for sensitive configuration
6. Implement network policies for service isolation
7. Add persistent volume claims for stateful components
8. Configure health checks (liveness/readiness probes)
9. Set up service accounts and RBAC policies
10. Create monitoring and logging configuration</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-8): Configure Kubernetes Deployments (Bolt - Kubernetes)</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>All services deploy successfully to Kubernetes, pods scale up/down with HPA, health checks pass consistently, services can communicate internally, external traffic routes correctly through ingress, secrets are mounted securely, and network policies prevent unauthorized access.</criterion>
    </acceptance_criteria>

    <decision_points>
        <decision_point id="d15" category="architecture" constraint="escalation" requires_approval="true">
            <description>Should we use a service mesh (Istio/Linkerd) for service-to-service communication?</description>
            <options><option>no-service-mesh</option><option>istio</option><option>linkerd</option></options>
        </decision_point>
        <decision_point id="d16" category="performance" constraint="open" requires_approval="false">
            <description>What resource requests and limits should be set for each service?</description>
            <options><option>minimal-resources</option><option>moderate-resources</option><option>resource-profiling-first</option></options>
        </decision_point>
    </decision_points>

    <validation>
        <command>kubectl apply --dry-run=client -f .</command>
        <command>helm lint charts/*</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
