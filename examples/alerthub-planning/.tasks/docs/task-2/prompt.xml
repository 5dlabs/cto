<?xml version="1.0" encoding="UTF-8"?>
<task id="2" priority="high" agent="rex">
    <meta>
        <title>Notification Router Core Service (Rex - Rust/Axum)</title>
        <priority>high</priority>
        <dependencies>task-1</dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Rust Engineer specializing in high-performance systems. Implement Task 2 with production-quality code.</role>

    <context>
        <overview>Build the high-performance notification routing service that validates, processes, and routes notifications with rate limiting and priority queues.</overview>
        <background>
            This task is part of AlertHub notification platform. Key resources:
            - PRD: .tasks/docs/prd.md (full requirements and architecture)
            - Related tasks: task-1
        </background>
    </context>

    <requirements>
        <description>Implement Axum web server with endpoints for notification submission, batch processing, status queries, and WebSocket support. Add PostgreSQL integration with sqlx, Redis rate limiting, Kafka event publishing, and Prometheus metrics. Include priority queue processing and deduplication logic.</description>
        <constraints>
            <constraint>Match existing codebase patterns and conventions</constraint>
            <constraint>Create PR with atomic, well-described commits</constraint>
            <constraint>Include unit tests for new functionality</constraint>
            <constraint>PR title format: feat(task-2): Notification Router Core Service (Rex - Rust/Axum)</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements from task description implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions and style guide</criterion>
        <criterion>API endpoints return correct responses, rate limiting blocks excess requests, notifications are persisted to PostgreSQL, events are published to Kafka, WebSocket connections receive real-time updates, and /metrics endpoint returns valid Prometheus format</criterion>
    </acceptance_criteria>

    <decision_points>
        <decision_point id="d3" category="error-handling" constraint="open" requires_approval="false">
            <description>Dead letter queue implementation strategy</description>
            <options><option>Redis-based dead letter queue</option><option>Kafka dead letter topic</option><option>PostgreSQL table for failed notifications</option></options>
        </decision_point>
        <decision_point id="d4" category="performance" constraint="soft" requires_approval="false">
            <description>Deduplication TTL configuration</description>
            <options><option>1 hour default TTL</option><option>configurable per tenant</option><option>24 hour fixed TTL</option></options>
        </decision_point>
    </decision_points>

    <validation>
        <command>cargo test --all</command>
        <command>cargo clippy -- -D warnings</command>
        <command>cargo fmt --check</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation of described functionality</deliverable>
        <deliverable>Unit tests covering new code paths</deliverable>
        <deliverable>Pull request with clear description</deliverable>
    </deliverables>
</task>
