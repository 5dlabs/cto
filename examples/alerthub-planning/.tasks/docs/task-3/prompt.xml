<?xml version="1.0" encoding="UTF-8"?>
<task id="3" priority="high" agent="nova">
    <meta>
        <title>Build Integration Service with Effect (Nova - Bun/Elysia)</title>
        <priority>high</priority>
        <dependencies>task-1</dependencies>
        <agent_hint>nova</agent_hint>
    </meta>

    <role>You are a TypeScript Engineer specializing in Effect and Bun/Elysia. Implement Task 3 with production-quality code.</role>

    <context>
        <overview>Create the integration service using Bun, Elysia, and Effect TypeScript to handle delivery to external channels (Slack, Discord, email, webhooks). Uses Effect for type-safe error handling and composable services.</overview>
        <background>
            This task is part of AlertHub notification platform.
            - PRD: .tasks/docs/prd.md
            - Related tasks: task-1
        </background>
    </context>

    <requirements>
        <description>1. Initialize Bun project with Elysia framework
2. Set up Effect services architecture (SlackService, DiscordService, EmailService)
3. Implement Effect Schema for request/response validation
4. Create MongoDB connection with Drizzle ORM
5. Build integration CRUD endpoints with Effect error handling
6. Implement Kafka consumer with Effect Stream
7. Add RabbitMQ task queue processing
8. Build delivery services with Effect.retry and exponential backoff
9. Add template rendering with Effect error handling
10. Implement OAuth2 token refresh with Effect.cached</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-3): Build Integration Service with Effect (Nova - Bun/Elysia)</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Service starts with Bun, Effect services initialize correctly, integration CRUD operations work, Kafka messages are consumed, deliveries succeed to test channels (Slack webhook, email), retry logic works on failures, and templates render correctly.</criterion>
    </acceptance_criteria>

    <decision_points>
        <decision_point id="d5" category="api-design" constraint="open" requires_approval="false">
            <description>Should integration configs be stored as flexible JSON or strongly typed schemas?</description>
            <options><option>flexible-json</option><option>typed-schemas</option><option>hybrid-approach</option></options>
        </decision_point>
        <decision_point id="d6" category="error-handling" constraint="soft" requires_approval="false">
            <description>How many retry attempts should be made for failed deliveries?</description>
            <options><option>3-attempts</option><option>5-attempts</option><option>configurable-per-channel</option></options>
        </decision_point>
    </decision_points>

    <validation>
        <command>bun test</command>
        <command>bun run lint</command>
        <command>bun run typecheck</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
