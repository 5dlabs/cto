<?xml version="1.0" encoding="UTF-8"?>
<task id="3" priority="high" agent="nova">
    <meta>
        <title>Integration Service with Effect (Nova - Bun/Elysia)</title>
        <priority>high</priority>
        <dependencies>task-1</dependencies>
        <agent_hint>nova</agent_hint>
    </meta>

    <role>You are a TypeScript Engineer specializing in Effect and Bun/Elysia. Implement Task 3 with production-quality code.</role>

    <context>
        <overview>Create the notification delivery service using Bun/Elysia with Effect TypeScript for type-safe error handling and channel integrations (Slack, Discord, email, webhooks).</overview>
        <background>
            This task is part of AlertHub notification platform. Key resources:
            - PRD: .tasks/docs/prd.md (full requirements and architecture)
            - Related tasks: task-1
        </background>
    </context>

    <requirements>
        <description>Build Elysia server with Effect services for each channel type. Implement SlackService, DiscordService, EmailService, and WebhookService using Effect.retry patterns. Add MongoDB integration with Drizzle ORM, Kafka consumer with Effect Stream, and RabbitMQ task queue processing.</description>
        <constraints>
            <constraint>Match existing codebase patterns and conventions</constraint>
            <constraint>Create PR with atomic, well-described commits</constraint>
            <constraint>Include unit tests for new functionality</constraint>
            <constraint>PR title format: feat(task-3): Integration Service with Effect (Nova - Bun/Elysia)</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements from task description implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions and style guide</criterion>
        <criterion>All integration endpoints respond correctly, Effect services handle errors gracefully, delivery retry logic works with exponential backoff, Kafka events are consumed successfully, and test deliveries reach external channels</criterion>
    </acceptance_criteria>

    <decision_points>
        <decision_point id="d5" category="error-handling" constraint="soft" requires_approval="false">
            <description>Retry strategy for failed deliveries</description>
            <options><option>exponential backoff with 3 retries</option><option>linear backoff with 5 retries</option><option>configurable retry policy per channel</option></options>
        </decision_point>
        <decision_point id="d6" category="api-design" constraint="open" requires_approval="false">
            <description>OAuth2 token storage and refresh mechanism</description>
            <options><option>store in MongoDB with Effect.cached</option><option>external token service</option><option>Redis-based token cache</option></options>
        </decision_point>
    </decision_points>

    <validation>
        <command>bun test</command>
        <command>bun run lint</command>
        <command>bun run typecheck</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation of described functionality</deliverable>
        <deliverable>Unit tests covering new code paths</deliverable>
        <deliverable>Pull request with clear description</deliverable>
    </deliverables>
</task>
