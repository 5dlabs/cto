## Codebase Patterns
- Intake CLI JSON parsing: Use `text.contains("```json")` not `text.find()` for code block detection
- Task file structure: `.tasks/tasks/tasks.json` with `metadata` and `tasks` arrays
- Agent hints: Task 1 should always be infrastructure (agentHint='bolt')
- Provider.rs fix commit: 9ee45367d - handles prefill-generated output from Claude

---

## 2026-01-14 13:44 - US-000
- Verified provider.rs fix from commit 9ee45367d works correctly
- Built intake locally with `cargo build -p intake`
- Confirmed tasks.json generated with valid JSON (15 tasks, not mixed with text)
- Task 1 has agentHint='bolt' (infrastructure) as required
- All 155 unit tests pass + 44 integration tests pass
- **Local intake test: PASS**

**Files verified:**
- crates/intake/src/ai/provider.rs (contains the fix)
- alerthub-e2e-test/.tasks/tasks/tasks.json (valid JSON with 15 tasks)

**Learnings for future iterations:**
- The intake CLI requires project initialization before parse-prd works standalone
- The `intake intake` command handles initialization automatically
- Provider.rs uses `text.contains()` for code block detection to handle AI responses with mixed text/JSON
---
