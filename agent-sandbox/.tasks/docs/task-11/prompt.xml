<?xml version="1.0" encoding="UTF-8"?>
<task id="11" priority="medium" agent="tap">
    <meta>
        <title>Add FCM push notification integration for mobile</title>
        <priority>medium</priority>
        <dependencies>6, 10</dependencies>
        <agent_hint>tap</agent_hint>
    </meta>

    <role>You are a Senior Mobile Engineer with expertise in React Native and cross-platform development. Implement Task 11 with production-quality code.</role>

    <context>
        <overview>Integrate Firebase Cloud Messaging for sending push notifications to mobile devices on task updates and mentions</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Add dependency: fcm = &quot;0.9&quot;
2. Create device_tokens table: (user_id uuid, device_token varchar, platform varchar, created_at timestamptz)
3. Add POST /api/users/devices endpoint to register FCM tokens
4. Create src/infra/push.rs:
   - Initialize FCM client with service account JSON from env
   - fn send_push(user_id, notification: PushNotification) -&gt; Result&lt;()&gt;
     * Query device_tokens for user
     * Send to all registered devices
     * Handle invalid tokens (remove from DB)
5. Integrate with notification system:
   - On task assignment, send push: &quot;You were assigned {task_title}&quot;
   - On mention, send push: &quot;{user} mentioned you&quot;
6. Add push_notifications bool to user_preferences</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-11): Add FCM push notification integration for mobile</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Manual test with real device token. Unit test notification payload construction. Integration test: register device, trigger task event, verify FCM API called. Test invalid token removal</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
