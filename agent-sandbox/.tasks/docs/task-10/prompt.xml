<?xml version="1.0" encoding="UTF-8"?>
<task id="10" priority="medium" agent="rex">
    <meta>
        <title>Implement email notification system for mentions and due dates</title>
        <priority>medium</priority>
        <dependencies>2, 6</dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Senior Rust Engineer with expertise in systems programming and APIs. Implement Task 10 with production-quality code.</role>

    <context>
        <overview>Set up email service integration with template rendering for task mentions and due date reminders, respecting user preferences</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Add dependencies: lettre = &quot;0.11&quot;, tera = &quot;1.19&quot;
2. Create src/infra/email.rs with SMTP configuration from env vars
3. Add user_preferences table: (user_id uuid PRIMARY KEY, email_mentions bool, email_due_dates bool, email_frequency varchar)
4. Create email templates in templates/:
   - mention_notification.html: &quot;@{user} mentioned you in {task}&quot;
   - due_date_reminder.html: &quot;Task {title} is due in {hours} hours&quot;
5. Implement notification queue in src/domain/notifications.rs:
   - fn send_mention_email(user_id, task_id, mentioner_id)
   - Check user preferences before sending
   - Parse task description for @mentions on task create/update
6. Create background job (tokio::spawn) to check due dates every hour:
   - Query tasks WHERE due_date BETWEEN NOW() AND NOW() + 24 hours
   - Send reminders to assignees if email_due_dates = true</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-10): Implement email notification system for mentions and due dates</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Unit test mention parsing. Integration test: create task with @mention, verify email sent (use mailhog in dev). Test preference controls prevent emails. Test due date reminder job with mock time</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
