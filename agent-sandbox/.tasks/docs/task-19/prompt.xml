<?xml version="1.0" encoding="UTF-8"?>
<task id="19" priority="high" agent="cipher">
    <meta>
        <title>Implement OAuth2 integration for Google and GitHub</title>
        <priority>high</priority>
        <dependencies>18</dependencies>
        <agent_hint>cipher</agent_hint>
    </meta>

    <role>You are a Senior Security Engineer with expertise in authentication and secure coding. Implement Task 19 with production-quality code.</role>

    <context>
        <overview>Add OAuth2 authentication flows for Google and GitHub providers with user account linking</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Add dependency: oauth2 = &quot;4.4&quot;
2. Create domain/oauth.rs with:
   - enum OAuthProvider { Google, GitHub }
   - struct OAuthConfig { client_id, client_secret, redirect_uri }
   - fn get_authorization_url(provider: OAuthProvider) -&gt; String
   - async fn exchange_code(provider: OAuthProvider, code: String) -&gt; Result&lt;UserInfo&gt;
3. Create api/oauth.rs handlers:
   - GET /api/auth/oauth/{provider} -&gt; redirect to provider
   - GET /api/auth/oauth/{provider}/callback?code=xxx -&gt; exchange code, create/link user, return JWT
4. Store OAuth tokens in users table with oauth_provider, oauth_id fields
5. Handle account linking: if email exists, link OAuth to existing account
6. Configure Google OAuth 2.0 client (https://console.cloud.google.com)
7. Configure GitHub OAuth app (https://github.com/settings/developers)</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-19): Implement OAuth2 integration for Google and GitHub</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Manual OAuth flow testing with real providers, verify account creation, test account linking, verify JWT returned after successful OAuth</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
