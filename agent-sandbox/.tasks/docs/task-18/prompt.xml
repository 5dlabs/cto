<?xml version="1.0" encoding="UTF-8"?>
<task id="18" priority="high" agent="cipher">
    <meta>
        <title>Implement JWT authentication with refresh token mechanism</title>
        <priority>high</priority>
        <dependencies>16, 17</dependencies>
        <agent_hint>cipher</agent_hint>
    </meta>

    <role>You are a Senior Security Engineer with expertise in authentication and secure coding. Implement Task 18 with production-quality code.</role>

    <context>
        <overview>Build JWT-based authentication system with access tokens (15min expiry) and refresh tokens (7 days), stored in Redis. Include token validation middleware.</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Add dependencies: jsonwebtoken = &quot;9&quot;, argon2 = &quot;0.5&quot;
2. Create domain/auth.rs with structs: Claims, TokenPair, RefreshToken
3. Implement JWT generation/validation functions:
   - generate_access_token(user_id, role) -&gt; Result&lt;String&gt;
   - generate_refresh_token(user_id) -&gt; Result&lt;String&gt;
   - validate_token(token: &amp;str) -&gt; Result&lt;Claims&gt;
4. Store refresh tokens in Redis with 7-day TTL: SET refresh:{token_id} {user_id} EX 604800
5. Create api/auth.rs with endpoints:
   - POST /api/auth/register (email, password)
   - POST /api/auth/login (email, password) -&gt; returns TokenPair
   - POST /api/auth/refresh (refresh_token) -&gt; returns new TokenPair
6. Implement Axum middleware in infra/middleware.rs:
   - RequireAuth extractor that validates Bearer token and injects Claims
7. Add JWT_SECRET and REFRESH_TOKEN_SECRET to config</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-18): Implement JWT authentication with refresh token mechanism</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Unit tests for token generation/validation with valid/expired/invalid tokens. Integration tests: register user, login, use access token, refresh token after expiry, verify old refresh token invalidated. Test middleware rejects requests without valid token.</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
