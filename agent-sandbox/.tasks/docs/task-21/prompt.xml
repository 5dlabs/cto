<?xml version="1.0" encoding="UTF-8"?>
<task id="21" priority="high" agent="rex">
    <meta>
        <title>Implement Team Management API endpoints</title>
        <priority>high</priority>
        <dependencies>18, 20</dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Senior Rust Engineer with expertise in systems programming and APIs. Implement Task 21 with production-quality code.</role>

    <context>
        <overview>Create CRUD endpoints for team operations including creation, retrieval, updates, and invite link generation</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Create domain/team.rs with:
   - struct Team { id, name, description, owner_id, member_count, created_at }
   - struct TeamRepository trait with CRUD methods
2. Implement infra/repositories/team_repo.rs with sqlx queries
3. Create api/teams.rs handlers:
   - POST /api/teams { name, description } -&gt; create team, add creator as owner
   - GET /api/teams/:id -&gt; join with team_members to get member_count
   - PATCH /api/teams/:id { name?, description? } -&gt; verify requester is owner/admin
   - POST /api/teams/:id/invite -&gt; generate uuid token, store in invites table with 7-day expiry, return invite URL
   - POST /api/teams/join/:token -&gt; verify token not expired, add user to team_members as member
4. Implement role-based authorization: only owner/admin can update team
5. Add validation: name length 3-100 chars, description max 500 chars</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-21): Implement Team Management API endpoints</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Integration tests for all endpoints, verify authorization checks, test expired invite rejection, verify member count accuracy, test concurrent team creation</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
