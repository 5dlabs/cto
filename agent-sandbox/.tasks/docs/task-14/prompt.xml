<?xml version="1.0" encoding="UTF-8"?>
<task id="14" priority="high" agent="rex">
    <meta>
        <title>Create observability infrastructure with Prometheus and structured logging</title>
        <priority>high</priority>
        <dependencies>1</dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Senior Rust Engineer with expertise in systems programming and APIs. Implement Task 14 with production-quality code.</role>

    <context>
        <overview>Implement metrics endpoint, structured JSON logging with trace IDs, and health check endpoints for production monitoring</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Add dependencies: tracing = &quot;0.1&quot;, tracing-subscriber = { version = &quot;0.3&quot;, features = [&quot;json&quot;] }, metrics = &quot;0.21&quot;, metrics-exporter-prometheus = &quot;0.13&quot;
2. Initialize tracing subscriber in main.rs:
   - JSON formatter with trace_id, span_id, level, message, timestamp
   - Filter by RUST_LOG env var
3. Create src/api/observability.rs:
   - GET /metrics: expose Prometheus metrics (request count, duration histogram, active connections)
   - GET /health/live: liveness probe (return 200 if server running)
   - GET /health/ready: readiness probe (check DB and Redis connectivity)
4. Add middleware to track metrics:
   - HTTP request counter by method, path, status
   - Request duration histogram
   - Active WebSocket connections gauge
5. Log key events: auth success/failure, task operations, errors with context
6. Generate trace_id per request, propagate through call chain</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-14): Create observability infrastructure with Prometheus and structured logging</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Unit test health check logic. Integration test: make requests, verify metrics incremented. Test /health/ready returns 503 when DB unavailable. Verify JSON logs parseable and contain trace_id</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
