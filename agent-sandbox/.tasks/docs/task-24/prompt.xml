<?xml version="1.0" encoding="UTF-8"?>
<task id="24" priority="medium" agent="rex">
    <meta>
        <title>Implement WebSocket endpoint for real-time task updates</title>
        <priority>medium</priority>
        <dependencies>23</dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Senior Rust Engineer with expertise in systems programming and APIs. Implement Task 24 with production-quality code.</role>

    <context>
        <overview>Create WebSocket connection handler that broadcasts task changes to team members using Redis pub/sub for horizontal scaling.</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Add dependency: axum-extra = { version = &quot;0.9&quot;, features = [&quot;typed-header&quot;] }
2. Create domain/events.rs:
   - TaskEvent enum (Created, Updated, Deleted, StatusChanged)
   - Serialize to JSON for Redis pub/sub
3. Implement infra/pubsub.rs:
   - subscribe_to_team(team_id) -&gt; redis::PubSub
   - publish_task_event(team_id, event: TaskEvent) -&gt; Result&lt;()&gt;
   - Channel format: team:{team_id}:tasks
4. Create api/ws.rs:
   - GET /api/teams/:team_id/ws (upgrade to WebSocket)
   - Validate JWT from query param or Sec-WebSocket-Protocol header
   - Verify user is team member
   - Subscribe to Redis channel for team
   - Forward events to WebSocket client
   - Handle ping/pong for connection health
5. Modify task handlers to publish events after mutations
6. Implement connection manager to track active connections per team
7. Add graceful shutdown handling</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-24): Implement WebSocket endpoint for real-time task updates</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Integration tests: connect WebSocket as team member, create task via REST API, verify WebSocket receives event. Test unauthorized connection rejected. Test connection survives network interruptions with ping/pong. Load test 1000 concurrent connections. Verify events only sent to team members.</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
