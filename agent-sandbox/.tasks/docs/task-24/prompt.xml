<?xml version="1.0" encoding="UTF-8"?>
<task id="24" priority="medium" agent="rex">
    <meta>
        <title>Implement WebSocket endpoint for real-time task updates</title>
        <priority>medium</priority>
        <dependencies>22</dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Senior Rust Engineer with expertise in systems programming and APIs. Implement Task 24 with production-quality code.</role>

    <context>
        <overview>Create WebSocket server for live task notifications using Redis pub/sub as message broker</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Add dependencies: axum-extra = { version = &quot;0.9&quot;, features = [&quot;ws&quot;] }, futures = &quot;0.3&quot;
2. Create api/websocket.rs with:
   - GET /api/ws -&gt; upgrade to WebSocket, authenticate via query param ?token=xxx
   - struct WsConnection { user_id, team_ids: Vec&lt;Uuid&gt;, sender: mpsc::Sender }
   - On connection: subscribe to Redis channels team:{team_id}:tasks for user&apos;s teams
3. Create infra/pubsub.rs:
   - async fn publish_task_event(redis: &amp;ConnectionManager, team_id: Uuid, event: TaskEvent)
   - async fn subscribe_to_team(redis: &amp;ConnectionManager, team_id: Uuid) -&gt; Receiver&lt;TaskEvent&gt;
4. Message format: { type: &quot;task.created&quot;|&quot;task.updated&quot;|&quot;task.deleted&quot;, task: Task }
5. Integrate with task API: publish events on create/update/delete
6. Handle connection limits: max 1000 concurrent connections, reject new connections if exceeded
7. Implement ping/pong for connection health</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-24): Implement WebSocket endpoint for real-time task updates</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>WebSocket client tests, verify events published on task operations, test connection limit enforcement, verify user only receives events for their teams, load test with 1000 connections</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
