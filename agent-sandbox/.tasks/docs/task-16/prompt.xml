<?xml version="1.0" encoding="UTF-8"?>
<task id="16" priority="high" agent="rex">
    <meta>
        <title>Setup Rust/Axum project foundation with PostgreSQL and Redis</title>
        <priority>high</priority>
        <dependencies></dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Senior Rust Engineer with expertise in systems programming and APIs. Implement Task 16 with production-quality code.</role>

    <context>
        <overview>Initialize the Rust project with Axum 0.7, configure PostgreSQL 15 with sqlx, and set up Redis 7 connections. Establish the directory structure following the architecture specification.</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Run `cargo init --name teamsync-api`
2. Add dependencies to Cargo.toml:
   - axum = &quot;0.7&quot;
   - tokio = { version = &quot;1&quot;, features = [&quot;full&quot;] }
   - sqlx = { version = &quot;0.7&quot;, features = [&quot;postgres&quot;, &quot;runtime-tokio-rustls&quot;, &quot;migrate&quot;] }
   - redis = { version = &quot;0.24&quot;, features = [&quot;tokio-comp&quot;, &quot;connection-manager&quot;] }
   - tower = &quot;0.4&quot;
   - tower-http = { version = &quot;0.5&quot;, features = [&quot;trace&quot;, &quot;cors&quot;] }
3. Create directory structure: src/{api, domain, infra}
4. Create config.rs for environment variables (DATABASE_URL, REDIS_URL)
5. Implement connection pools in infra/db.rs and infra/redis.rs
6. Create main.rs with basic Axum router and health check endpoint
7. Add .env.example with required environment variables</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-16): Setup Rust/Axum project foundation with PostgreSQL and Redis</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Verify cargo build succeeds, health check endpoint returns 200, database and Redis connections establish successfully with docker-compose up</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
