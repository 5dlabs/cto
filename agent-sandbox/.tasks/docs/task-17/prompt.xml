<?xml version="1.0" encoding="UTF-8"?>
<task id="17" priority="high" agent="rex">
    <meta>
        <title>Design and implement database schema with migrations</title>
        <priority>high</priority>
        <dependencies>16</dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Senior Rust Engineer with expertise in systems programming and APIs. Implement Task 17 with production-quality code.</role>

    <context>
        <overview>Create PostgreSQL schema for teams, users, tasks, invites, and audit tables with proper indexes and constraints</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Create migration files using sqlx migrate add
2. Schema tables:
   - users: id (uuid), email, password_hash, oauth_provider, created_at, deleted_at
   - teams: id (uuid), name, description, owner_id (fk users), created_at, deleted_at
   - team_members: team_id, user_id, role (enum: owner/admin/member/viewer), joined_at
   - tasks: id (uuid), team_id (fk), title, description, assignee_id (fk users), status (enum), due_date, created_at, updated_at, deleted_at
   - invites: id (uuid), team_id (fk), token, expires_at, created_by (fk users)
3. Add indexes: tasks(team_id, status), tasks(assignee_id), team_members(user_id), invites(token)
4. Add CHECK constraints for status enum values
5. Setup foreign key cascades appropriately</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-17): Design and implement database schema with migrations</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Run sqlx migrate run, verify schema with \d commands, test constraint violations, verify indexes exist</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
