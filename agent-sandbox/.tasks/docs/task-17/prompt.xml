<?xml version="1.0" encoding="UTF-8"?>
<task id="17" priority="high" agent="rex">
    <meta>
        <title>Design and implement database schema with migrations</title>
        <priority>high</priority>
        <dependencies>16</dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Senior Rust Engineer with expertise in systems programming and APIs. Implement Task 17 with production-quality code.</role>

    <context>
        <overview>Create PostgreSQL schema for users, teams, team_members, tasks, and invite_links tables with proper indexes and constraints. Implement soft delete pattern for tasks.</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Create migrations directory: migrations/
2. Migration 001_create_users.sql:
   - id (UUID PK), email (UNIQUE), password_hash, oauth_provider, oauth_id, created_at, updated_at
3. Migration 002_create_teams.sql:
   - id (UUID PK), name, description, owner_id (FK users), created_at, updated_at
4. Migration 003_create_team_members.sql:
   - id (UUID PK), team_id (FK teams), user_id (FK users), role (ENUM: owner, admin, member, viewer), joined_at
   - UNIQUE constraint on (team_id, user_id)
5. Migration 004_create_tasks.sql:
   - id (UUID PK), team_id (FK teams), title, description, assignee_id (FK users), status (ENUM: todo, in_progress, done), due_date, deleted_at (nullable), created_at, updated_at
   - Index on (team_id, deleted_at, status)
6. Migration 005_create_invite_links.sql:
   - id (UUID PK), team_id (FK teams), token (UNIQUE), expires_at, created_by (FK users), created_at
7. Run migrations with sqlx migrate run</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-17): Design and implement database schema with migrations</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Run sqlx migrate run and verify all tables exist with correct schema using psql. Test foreign key constraints by attempting invalid inserts. Verify indexes exist with EXPLAIN ANALYZE queries.</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
