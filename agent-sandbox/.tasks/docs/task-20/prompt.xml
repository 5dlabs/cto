<?xml version="1.0" encoding="UTF-8"?>
<task id="20" priority="high" agent="cipher">
    <meta>
        <title>Implement rate limiting with Redis token bucket algorithm</title>
        <priority>high</priority>
        <dependencies>16, 18</dependencies>
        <agent_hint>cipher</agent_hint>
    </meta>

    <role>You are a Senior Security Engineer with expertise in authentication and secure coding. Implement Task 20 with production-quality code.</role>

    <context>
        <overview>Create middleware for rate limiting using Redis-based token bucket: 100 req/min for authenticated users, 20 req/min for anonymous.</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Create infra/rate_limit.rs with TokenBucket struct
2. Implement token bucket algorithm:
   - Key format: rate_limit:{user_id|ip}:{window}
   - Use Redis INCR with EXPIRE for atomic operations
   - Lua script for atomic check-and-decrement:
     ```lua
     local current = redis.call(&apos;GET&apos;, KEYS[1])
     if not current then
       redis.call(&apos;SET&apos;, KEYS[1], ARGV[1] - 1, &apos;EX&apos;, ARGV[2])
       return 1
     elseif tonumber(current) &gt; 0 then
       redis.call(&apos;DECR&apos;, KEYS[1])
       return 1
     else
       return 0
     end
     ```
3. Create RateLimitLayer middleware:
   - Extract user_id from Claims or use IP address
   - Check bucket, decrement if available
   - Return 429 Too Many Requests with Retry-After header if exhausted
4. Apply different limits based on authentication status
5. Add X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset headers</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-20): Implement rate limiting with Redis token bucket algorithm</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Unit tests for token bucket logic with mocked Redis. Integration tests: make 100 requests as authenticated user, verify 101st fails. Test anonymous limit of 20. Verify rate limit resets after window. Test concurrent requests don&apos;t exceed limit.</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
