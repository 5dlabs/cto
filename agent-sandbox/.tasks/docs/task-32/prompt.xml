<?xml version="1.0" encoding="UTF-8"?>
<task id="32" priority="low" agent="blaze">
    <meta>
        <title>Implement dark/light theme toggle with persistence</title>
        <priority>low</priority>
        <dependencies>30</dependencies>
        <agent_hint>blaze</agent_hint>
    </meta>

    <role>You are a Senior Frontend Engineer with expertise in React, TypeScript, and modern UI/UX. Implement Task 32 with production-quality code.</role>

    <context>
        <overview>Add theme switching functionality with system preference detection and localStorage persistence across the React dashboard.</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Create src/contexts/ThemeContext.tsx:
   - ThemeProvider with state: &apos;light&apos; | &apos;dark&apos; | &apos;system&apos;
   - Detect system preference with window.matchMedia(&apos;(prefers-color-scheme: dark)&apos;)
   - Listen for system theme changes
   - Persist preference in localStorage
2. Create src/components/ThemeToggle.tsx:
   - Button with sun/moon/auto icons
   - Cycles through light -&gt; dark -&gt; system
3. Update tailwind.config.js:
   - Enable dark mode: &apos;class&apos;
4. Add dark: variants to all components:
   - Dark background colors
   - Dark text colors
   - Dark borders and shadows
5. Apply theme class to document.documentElement:
   - &lt;html className={theme === &apos;dark&apos; ? &apos;dark&apos; : &apos;&apos;}&gt;
6. Create CSS variables for theme colors in src/index.css:
   - --color-background, --color-text, etc.
7. Add ThemeToggle to app header</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-32): Implement dark/light theme toggle with persistence</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Unit tests for ThemeContext. Test system preference detection. Verify localStorage persistence across sessions. Test theme toggle cycles correctly. Manual testing: verify all components readable in both themes. Test automatic theme switch when system preference changes. Verify no flash of unstyled content on load.</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
