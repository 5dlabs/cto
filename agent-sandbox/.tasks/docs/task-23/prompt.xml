<?xml version="1.0" encoding="UTF-8"?>
<task id="23" priority="medium" agent="rex">
    <meta>
        <title>Implement scheduled cleanup job for soft-deleted tasks</title>
        <priority>medium</priority>
        <dependencies>22</dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Senior Rust Engineer with expertise in systems programming and APIs. Implement Task 23 with production-quality code.</role>

    <context>
        <overview>Create background job to permanently delete tasks older than 30 days from soft deletion</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Add dependency: tokio-cron-scheduler = &quot;0.9&quot;
2. Create infra/jobs/cleanup.rs with:
   - async fn cleanup_deleted_tasks(pool: &amp;PgPool) -&gt; Result&lt;u64&gt;
   - SQL: DELETE FROM tasks WHERE deleted_at IS NOT NULL AND deleted_at &lt; NOW() - INTERVAL &apos;30 days&apos;
3. In main.rs, spawn background task:
   - tokio::spawn(async move { loop { cleanup_deleted_tasks(&amp;pool).await; tokio::time::sleep(Duration::from_secs(86400)).await; } })
4. Log cleanup results with count of deleted tasks
5. Add metrics counter for deleted tasks</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-23): Implement scheduled cleanup job for soft-deleted tasks</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Unit test with mock database, integration test with test data, verify tasks deleted after 30 days, verify tasks within 30 days retained</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
