<?xml version="1.0" encoding="UTF-8"?>
<task id="28" priority="high" agent="bolt">
    <meta>
        <title>Create Docker multi-stage build and Kubernetes manifests</title>
        <priority>high</priority>
        <dependencies>27</dependencies>
        <agent_hint>bolt</agent_hint>
    </meta>

    <role>You are a Senior DevOps Engineer with expertise in Kubernetes, GitOps, and CI/CD. Implement Task 28 with production-quality code.</role>

    <context>
        <overview>Build optimized Docker image with multi-stage build and create Kubernetes deployment with HPA, ConfigMaps, and Secrets.</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Create Dockerfile:
   ```dockerfile
   FROM rust:1.75 as builder
   WORKDIR /app
   COPY Cargo.* ./
   RUN mkdir src &amp;&amp; echo &quot;fn main() {}&quot; &gt; src/main.rs &amp;&amp; cargo build --release
   COPY src ./src
   RUN touch src/main.rs &amp;&amp; cargo build --release
   
   FROM debian:bookworm-slim
   RUN apt-get update &amp;&amp; apt-get install -y ca-certificates &amp;&amp; rm -rf /var/lib/apt/lists/*
   COPY --from=builder /app/target/release/teamsync-api /usr/local/bin/
   EXPOSE 8080
   CMD [&quot;teamsync-api&quot;]
   ```
2. Create infra/k8s/namespace.yaml
3. Create infra/k8s/configmap.yaml (non-sensitive config)
4. Create infra/k8s/secret.yaml (JWT_SECRET, DB credentials, etc.)
5. Create infra/k8s/deployment.yaml:
   - 3 replicas, resource limits (500m CPU, 512Mi memory)
   - livenessProbe: /health/live, readinessProbe: /health/ready
   - Environment variables from ConfigMap and Secret
6. Create infra/k8s/service.yaml (ClusterIP, port 8080)
7. Create infra/k8s/hpa.yaml:
   - Min 3, max 10 replicas
   - Target CPU 70%, memory 80%
8. Create infra/k8s/ingress.yaml (HTTPS with cert-manager)</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-28): Create Docker multi-stage build and Kubernetes manifests</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Build Docker image, verify size &lt; 100MB. Run container locally, test health endpoints. Deploy to local k8s (minikube/kind), verify pods start. Test HPA by generating load. Verify rolling updates work. Test ConfigMap/Secret changes trigger restart.</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
