<?xml version="1.0" encoding="UTF-8"?>
<task id="28" priority="medium" agent="blaze">
    <meta>
        <title>Implement team activity feed and member management UI</title>
        <priority>medium</priority>
        <dependencies>27</dependencies>
        <agent_hint>blaze</agent_hint>
    </meta>

    <role>You are a Senior Frontend Engineer with expertise in React, TypeScript, and modern UI/UX. Implement Task 28 with production-quality code.</role>

    <context>
        <overview>Create activity timeline showing recent team actions and member management interface with role assignment</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Create components:
   - src/components/ActivityFeed.tsx: timeline of recent task/member events
   - src/components/MemberList.tsx: table of team members with roles
   - src/components/InviteModal.tsx: generate and display invite link
2. Add activity_log table to schema: id, team_id, user_id, action (enum), entity_type, entity_id, created_at
3. Create API endpoint GET /api/teams/:id/activity -&gt; return last 50 events
4. Log activities in task/member operations: task.created, task.updated, member.joined, member.role_changed
5. Member management features:
   - Display member list with role badges
   - Owner/admin can change member roles via PATCH /api/teams/:team_id/members/:user_id
   - Owner/admin can remove members via DELETE /api/teams/:team_id/members/:user_id
6. Implement invite link generation: click button, show modal with copyable link
7. Style with Tailwind: use timeline component for activity feed</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-28): Implement team activity feed and member management UI</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Component tests for activity feed rendering, test role change authorization, verify activity logging on operations, E2E test for invite flow</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
