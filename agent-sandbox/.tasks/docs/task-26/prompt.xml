<?xml version="1.0" encoding="UTF-8"?>
<task id="26" priority="medium" agent="tap">
    <meta>
        <title>Implement FCM push notifications for mobile</title>
        <priority>medium</priority>
        <dependencies>23</dependencies>
        <agent_hint>tap</agent_hint>
    </meta>

    <role>You are a Senior Mobile Engineer with expertise in React Native and cross-platform development. Implement Task 26 with production-quality code.</role>

    <context>
        <overview>Integrate Firebase Cloud Messaging for mobile push notifications with device token management and notification delivery.</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Add dependency: fcm = &quot;0.9&quot;
2. Create migrations/006_device_tokens.sql:
   - id (UUID PK), user_id (FK users), token (UNIQUE), platform (ENUM: ios, android), created_at, last_used_at
3. Implement infra/push.rs:
   - FcmService with API key from config
   - send_push_notification(user_id, title, body, data) -&gt; Result&lt;()&gt;
   - Fetch active device tokens for user (last_used_at within 30 days)
   - Handle invalid token responses, delete from DB
4. Create api/devices.rs:
   - POST /api/devices/register (requires auth) -&gt; stores FCM token
   - DELETE /api/devices/:token (requires auth) -&gt; removes token
5. Integrate with task events:
   - Send push on task assignment
   - Send push on @mention
   - Respect user notification preferences
6. Add FCM_API_KEY to config
7. Include deep link data in notification payload: {task_id, team_id}</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-26): Implement FCM push notifications for mobile</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Integration tests with FCM sandbox. Register device token, create task assigned to user, verify push sent. Test invalid token cleanup. Verify preferences disable push. Test multiple devices per user. Manual testing on iOS/Android devices with deep links.</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
