<?xml version="1.0" encoding="UTF-8"?>
<task id="26" priority="medium" agent="tap">
    <meta>
        <title>Implement FCM push notification integration</title>
        <priority>medium</priority>
        <dependencies>25</dependencies>
        <agent_hint>tap</agent_hint>
    </meta>

    <role>You are a Senior Mobile Engineer with expertise in React Native and cross-platform development. Implement Task 26 with production-quality code.</role>

    <context>
        <overview>Add Firebase Cloud Messaging support for mobile push notifications with device token management</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Add dependency: fcm = &quot;0.9&quot;
2. Add device_tokens table: id, user_id, token, platform (ios/android), created_at
3. Create api/devices.rs:
   - POST /api/devices/register { token, platform } -&gt; store device token for authenticated user
   - DELETE /api/devices/:id -&gt; remove device token
4. Create infra/push.rs with:
   - struct PushService { fcm_client: fcm::Client }
   - async fn send_push(user_id: Uuid, notification: PushNotification) -&gt; Result&lt;()&gt;
   - Query device_tokens for user, send to all devices
5. Integrate with task events: on task assignment/mention, send push if user enabled
6. Handle FCM errors: remove invalid tokens from database
7. Configure FCM via FIREBASE_SERVER_KEY env var</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-26): Implement FCM push notification integration</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Unit tests with mock FCM client, integration tests for device registration, verify push sent on task events, test invalid token cleanup</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
