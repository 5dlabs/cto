---
# Consolidated infrastructure namespace
# Contains: All cluster operators, controllers, ingress, DNS, and certificate management
# Replaces: operators, cert-manager, ingress-nginx, external-dns
apiVersion: v1
kind: Namespace
metadata:
  name: infra
  labels:
    app.kubernetes.io/name: infra
    app.kubernetes.io/part-of: platform
    app.kubernetes.io/component: infrastructure

