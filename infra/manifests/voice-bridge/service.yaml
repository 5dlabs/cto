---
apiVersion: v1
kind: Service
metadata:
  name: voice-bridge
  namespace: cto
  labels:
    app.kubernetes.io/name: voice-bridge
    app.kubernetes.io/part-of: morgan-voice
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: voice-bridge
  ports:
    - name: http
      port: 8090
      targetPort: 8090
      protocol: TCP
