apiVersion: v1
kind: Service
metadata:
  name: discord-bridge-http
  namespace: bots
  labels:
    app: discord-bridge
    component: http-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3200
      targetPort: 3200
      protocol: TCP
  selector:
    app: discord-bridge
