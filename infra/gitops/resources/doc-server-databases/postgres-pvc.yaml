---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgdata-vector-postgres-0
  namespace: databases
  labels:
    app.kubernetes.io/name: vector-postgres
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: doc-server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: local-path

