apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - httproute.yaml
  - eventsource.yaml
  - eventbus.yaml
  - sensor.yaml
  - externalsecret.yaml

secretGenerator:
  - name: github-webhook-secret
    namespace: argo
    literals: []
generatorOptions:
  disableNameSuffixHash: true

