---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cto

resources:
  - rbac.yaml
  - configmap.yaml
  - job.yaml

commonLabels:
  app.kubernetes.io/name: ralph-runner
  app.kubernetes.io/component: lifecycle-test
  app.kubernetes.io/part-of: cto-platform
