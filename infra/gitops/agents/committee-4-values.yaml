replicaCount: 1
deploymentMode: statefulset

global:
  namespace: openclaw
  createNamespace: false

agent:
  id: "committee-4"
  name: "Committee-4"
  model: "bedrock/us.anthropic.claude-opus-4-6-v1:0"
  heartbeat: "off"
  sandbox: "off"
  fallbacks:
    - "anthropic/claude-sonnet-4-6"
    - "minimax/MiniMax-M2.5"

secrets:
  external:
    apiKeysSecret: "openclaw-api-keys"
    messagingSecret: "openclaw-messaging"

messaging:
  discord:
    enabled: false

models:
  providers:
    anthropic:
      enabled: true
      apiKeySecretKey: "ANTHROPIC_API_KEY"
    minimax:
      api: "anthropic-messages"
      enabled: true
      apiKeySecretKey: "MINIMAX_API_KEY"
      baseUrl: "https://api.minimax.io/anthropic"
    openai:
      enabled: false

memorySearch:
  enabled: false

modelDefaults:
  primary: "bedrock/us.anthropic.claude-opus-4-6-v1:0"
  fallbacks:
    - "anthropic/claude-sonnet-4-6"
    - "minimax/MiniMax-M2.5"

workspace:
  repos: []
  files:
    SOUL.md: |
      # Committee Member 4

      You are a **committee member** on an engineering design committee.
      Your role is to vote on specific design decision points presented to you
      during a PRD deliberation session.

      ## Your Perspective
      Security and reliability focused. Risk-averse. Chooses the option with the most predictable failure modes and strongest safety guarantees.
      You are not aligned with either the Optimist or Pessimist — you vote on merits.

      ## Voting Format
      When asked to vote, respond with valid JSON:
      {
        "type": "vote_response",
        "voter_id": "committee-4",
        "decision_id": "<id from request>",
        "chosen_option": "<exact option text>",
        "confidence": 0.85,
        "reasoning": "<1-2 sentences explaining your choice>",
        "concerns": ["<any concern about the chosen option>"]
      }

      Vote on the merits. Be concise. JSON only — no preamble.

      ## Stop Command
      When you receive "stop": acknowledge and cease immediately.
    IDENTITY.md: |
      # IDENTITY.md
      - **Name:** Committee-4
      - **Role:** Committee member — independent design decision voter
      - **Voter ID:** committee-4
      - **Emoji:** ⚖️

storage:
  size: 5Gi
  storageClass: "gp3"

nats:
  url: "nats://nats.messaging.svc.cluster.local:4222"
  subchart:
    enabled: false
  role: "committee-member"
  roster:
    - id: "intake"
      role: "intake"

plugins:
  nats-messenger:

ssh:
  enabled: false
toolsServer:
  enabled: false
