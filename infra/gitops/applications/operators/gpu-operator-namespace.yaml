---
# Argo CD Application to manage gpu-operator namespace labels (PSA)
# Must sync before the main nvidia-gpu-operator app (sync-wave -4 vs -3)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gpu-operator-namespace
  namespace: argocd
  labels:
    app.kubernetes.io/name: gpu-operator-namespace
    app.kubernetes.io/part-of: platform
  annotations:
    # Deploy before nvidia-gpu-operator app (-3) to ensure namespace has correct labels
    argocd.argoproj.io/sync-wave: "-4"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: https://github.com/5dlabs/cto
    targetRevision: develop
    path: infra/cluster-config
    directory:
      include: gpu-operator-namespace.yaml
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=true
      - PrunePropagationPolicy=foreground
      - RespectIgnoreDifferences=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 2m
