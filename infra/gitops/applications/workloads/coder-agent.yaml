---
# =============================================================================
# Coder Agent — ArgoCD Application
# =============================================================================
# Deploys the Coder agent as a standalone deployment in the cto namespace.
# General-purpose coding agent focused on e2e testing of intake, work, play,
# and lobster workflows. Uses Claude Code as primary CLI with agent teams.
# =============================================================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cto-coder
  namespace: argocd
  labels:
    app.kubernetes.io/name: cto-coder
    app.kubernetes.io/part-of: cto-platform
    app.kubernetes.io/component: coder
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  sources:
    - repoURL: https://github.com/5dlabs/cto.git
      targetRevision: main
      path: infra/charts/openclaw-agent
      helm:
        releaseName: cto-coder
        valueFiles:
          - ../../gitops/agents/coder-values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: cto

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: "5s"
        factor: 2
        maxDuration: "3m"
