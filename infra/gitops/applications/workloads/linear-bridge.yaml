---
# Linear Bridge
# =============================================================================
# NATS-to-Linear conversation bridge for OpenClaw agent visibility.
# Taps agent.> wildcard and relays inter-agent messages to Linear issue comments.

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: linear-bridge
  namespace: argocd
  labels:
    app.kubernetes.io/name: linear-bridge
    app.kubernetes.io/part-of: openclaw
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform

  source:
    repoURL: https://gitlab.5dlabs.ai/5dlabs/cto.git
    targetRevision: main
    path: infra/manifests/linear-bridge

  destination:
    server: https://kubernetes.default.svc
    namespace: bots

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false

    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - PrunePropagationPolicy=foreground
      - RespectIgnoreDifferences=true

    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 5
