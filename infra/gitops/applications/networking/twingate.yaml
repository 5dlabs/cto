---
# Argo CD Application for Twingate Operator-based Resources
# NOTE: Currently empty - connector deployed via twingate-connector Helm app.
# Operator-based CRD management is pending proper operator installation.
# See infra/gitops/manifests/twingate/kustomization.yaml for details.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: twingate
  namespace: argocd
  labels:
    app.kubernetes.io/name: twingate
    app.kubernetes.io/part-of: platform
    app.kubernetes.io/component: networking
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: https://github.com/5dlabs/cto
    path: infra/gitops/manifests/twingate
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: operators
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: true
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 5
