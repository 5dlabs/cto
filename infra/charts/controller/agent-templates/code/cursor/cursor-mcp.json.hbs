{
  "mcpServers": {
    "toolman": {
      "command": "toolman",
      "args": ["--url", "{{toolman.url}}", "--working-dir", "/workspace"],
      "env": {
        "TOOLMAN_SERVER_URL": "{{toolman.url}}"
      }
    }
  }
}

