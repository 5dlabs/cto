#!/bin/sh
# Claude Watch Remediation - Rex
# This is a placeholder for Claude CLI support in Watch workflows.
# Currently, Watch workflows use Factory CLI (droid exec) as the primary CLI.
# Claude support can be added by copying the pattern from code/claude/container-rex.sh.hbs

echo '════════════════════════════════════════════════════════════════'
echo '║        REX E2E WATCH REMEDIATION (CLAUDE) STARTING          ║'
echo '════════════════════════════════════════════════════════════════'
echo "🎯 Agent: {{github_app}}"
echo "📋 Task ID: {{task_id}}"
echo "⚠️  Claude CLI support for Watch is not yet implemented"
echo "    Please use Factory CLI (droid exec) for Watch workflows"
exit 1

