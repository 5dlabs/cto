image:
  repository: ghcr.io/5dlabs/morgan-avatar-minimal
  tag: v0.1.0
  pullPolicy: Always

replicaCount: 1

resources:
  requests:
    cpu: "200m"
    memory: "256Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"
