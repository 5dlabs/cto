apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "musetalk-worker.fullname" . }}
  labels:
    {{- include "musetalk-worker.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "musetalk-worker.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- toYaml .Values.podAnnotations | nindent 8 }}
      labels:
        {{- include "musetalk-worker.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "musetalk-worker.serviceAccountName" . }}
      {{- with .Values.priorityClassName }}
      priorityClassName: {{ . | quote }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: fix-symlink
          image: busybox
          command:
            - sh
            - -c
            - |
              # Create musetalkV15 symlink for upstream compatibility
              if [ -d /models/musetalk ] && [ ! -e /models/musetalkV15 ]; then
                ln -s /models/musetalk /models/musetalkV15
                echo "Created musetalkV15 -> musetalk symlink"
              fi
              ls -la /models/
          volumeMounts:
            - name: model-cache
              mountPath: {{ .Values.model.cacheDir }}
        - name: download-models
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["python3", "/app/download_models.py"]
          env:
            - name: HF_MODEL_REPO
              value: {{ .Values.model.repo | quote }}
            - name: MODEL_CACHE_DIR
              value: {{ .Values.model.cacheDir | quote }}
            {{- if .Values.huggingface.existingSecret }}
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.huggingface.existingSecret }}
                  key: {{ .Values.huggingface.secretKey }}
            {{- end }}
          volumeMounts:
            - name: model-cache
              mountPath: {{ .Values.model.cacheDir }}
          resources:
            requests:
              cpu: "1"
              memory: 4Gi
            limits:
              cpu: "4"
              memory: 8Gi
      containers:
        - name: worker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: health
              containerPort: 8080
              protocol: TCP
            {{- if .Values.metrics.enabled }}
            - name: metrics
              containerPort: {{ .Values.metrics.port }}
              protocol: TCP
            {{- end }}
          env:
            - name: NATS_URL
              value: {{ .Values.nats.url | quote }}
            - name: NATS_SUBJECT
              value: {{ .Values.nats.subject | quote }}
            - name: NATS_RESULT_SUBJECT
              value: {{ .Values.nats.resultSubject | quote }}
            - name: NATS_QUEUE
              value: {{ .Values.nats.queue | quote }}
            - name: NATS_STREAM
              value: {{ .Values.nats.stream | quote }}
            - name: NATS_CONSUMER
              value: {{ .Values.nats.consumer | quote }}
            - name: NATS_ACK_WAIT
              value: {{ .Values.nats.ackWait | quote }}
            - name: NATS_MAX_DELIVER
              value: {{ .Values.nats.maxDeliver | quote }}
            - name: METRICS_PORT
              value: {{ .Values.metrics.port | quote }}
            - name: MODEL_CACHE_DIR
              value: {{ .Values.model.cacheDir | quote }}
            - name: MODEL_DTYPE
              value: {{ .Values.model.dtype | quote }}
            - name: OUTPUT_BUCKET
              value: {{ .Values.output.bucket | quote }}
            - name: OUTPUT_ENDPOINT
              value: {{ .Values.output.endpoint | quote }}
            - name: OUTPUT_REGION
              value: {{ .Values.output.region | quote }}
            - name: OUTPUT_PREFIX
              value: {{ .Values.output.prefix | quote }}
            - name: CACHE_ENABLED
              value: {{ .Values.output.cacheEnabled | quote }}
            - name: CACHE_TTL_DAYS
              value: {{ .Values.output.cacheTTLDays | quote }}
            {{- if .Values.huggingface.existingSecret }}
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.huggingface.existingSecret }}
                  key: {{ .Values.huggingface.secretKey }}
            {{- end }}
            {{- if .Values.output.existingSecret }}
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.output.existingSecret }}
                  key: {{ .Values.output.accessKeyKey }}
                  optional: true
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.output.existingSecret }}
                  key: {{ .Values.output.secretKeyKey }}
                  optional: true
            - name: AWS_SESSION_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.output.existingSecret }}
                  key: {{ .Values.output.sessionTokenKey }}
                  optional: true
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          volumeMounts:
            - name: model-cache
              mountPath: {{ .Values.model.cacheDir }}
            - name: render-scratch
              mountPath: /tmp/renders
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: model-cache
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "musetalk-worker.fullname" . }}-models
          {{- else }}
          emptyDir:
            sizeLimit: 20Gi
          {{- end }}
        - name: render-scratch
          emptyDir:
            sizeLimit: 10Gi
