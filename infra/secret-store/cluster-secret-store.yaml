---
# DEPRECATED: This Kubernetes-backed ClusterSecretStore is being replaced by Vault.
# Use vault-cluster-secret-store.yaml instead.
# This file will be removed after migration is verified.
# See: docs/runbooks/vault-operations.md
#
# Original description:
# ClusterSecretStore for cross-namespace access to secret-store namespace
# This must be created before ExternalSecrets can reference it
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: secret-store
spec:
  provider:
    kubernetes:
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          key: ca.crt
          namespace: kube-system
      auth:
        serviceAccount:
          name: external-secrets
          namespace: external-secrets-system
      remoteNamespace: secret-store
