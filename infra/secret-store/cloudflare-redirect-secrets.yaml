---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cloudflare-redirect-credentials
  namespace: external-dns
  labels:
    app.kubernetes.io/name: cloudflare-redirect-credentials
    app.kubernetes.io/part-of: platform
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: secret-store
    kind: ClusterSecretStore
  target:
    name: cloudflare-redirect-credentials
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        api-token: "{{ .cloudflare_redirect_api_token }}"
  data:
    - secretKey: cloudflare_redirect_api_token
      remoteRef:
        key: cloudflare
        property: redirect_api_token
