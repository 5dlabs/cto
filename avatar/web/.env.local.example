# LiveKit server credentials used by app/api/token/route.ts
LIVEKIT_URL=wss://lk.5dlabs.ai
LIVEKIT_API_KEY=lkapi_your_key
LIVEKIT_API_SECRET=your_secret

# --- Optional: TalkingHead 3D runtime ---
# Set to `talkinghead` to render the WebGL avatar; omit for the deterministic portrait.
# Lip-sync is driven in-browser by HeadAudio on the LiveKit audio stream — no
# voice-bridge or server TTS alignment is needed for mouth movement.
NEXT_PUBLIC_AVATAR_RUNTIME=talkinghead
# Ready Player Me .glb with ARKit-52 + Oculus-15 visemes baked in. The default
# below is Morgan's WS-B1 stand-in; the canine anthro rig lands in WS-B2.
# If unset, `config/morgan.ts` supplies the same URL as a fallback so the
# TalkingHead runtime always has an avatar to drive.
NEXT_PUBLIC_AVATAR_GLB_URL=https://models.readyplayer.me/64bfa15f0e72c63d7c3934a6.glb?morphTargets=ARKit,Oculus%20Visemes,mouthOpen,mouthSmile,eyesClosed,eyesLookUp,eyesLookDown
