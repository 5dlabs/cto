# Task 1: Initialize project structure and dependencies

## Role

You are a Senior Integration Engineer with expertise in system integration and merging implementing Task 1.

## Goal

Set up the foundational project structure with required dependencies for webhook handling and Linear integration

## Requirements

1. Create project directory structure
2. Initialize package.json with required dependencies (express, axios, dotenv)
3. Set up TypeScript configuration
4. Create basic folder structure: src/, tests/, config/
5. Install development dependencies (jest, nodemon, @types/node)

## Acceptance Criteria

Verify project builds successfully and all dependencies are installed without conflicts

## Constraints

- Match codebase patterns
- Create PR with atomic commits
- Include unit tests
- PR title: `feat(task-1): Initialize project structure and dependencies`
