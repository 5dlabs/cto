# Optional: inject OP_SERVICE_ACCOUNT_TOKEN for unattended scripts (e.g. google-oauth-json-to-1password.sh).
# Copy to gitignored google-oauth-service-account.env.op and set the op:// line.
#
# Replace YOUR_VAULT and item title with real values from: op vault list && op item list
# Example (illustration only — use your names):
#   OP_SERVICE_ACCOUNT_TOKEN=op://Private/Service Account CTO/credential
#
# For normal use you do NOT need this file: use eval "$(op signin)" and leave OP_SERVICE_ACCOUNT_TOKEN unset.
#
# Vault access: in 1Password.com admin, grant this service account "View" + "Create items" on the vault.

OP_SERVICE_ACCOUNT_TOKEN=
