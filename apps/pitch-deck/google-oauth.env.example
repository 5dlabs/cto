# Copy to .env.local (or set in Cloudflare Pages → Environment variables).
# Web OAuth client ID; enable Drive API; OAuth scope: drive.file
# https://console.cloud.google.com/apis/credentials
#
# OAuth consent screen (public URLs after deploy — not linked on the deck UI):
#   Privacy  https://pitch.5dlabs.ai/privacy/
#   Terms    https://pitch.5dlabs.ai/terms/
NEXT_PUBLIC_GOOGLE_OAUTH_CLIENT_ID=
