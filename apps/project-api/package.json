{
  "name": "project-api",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP sidecar on the Morgan OpenClaw pod — exposes /workspace/repos/ to the cto-app UI and Morgan agent (list, create via GitHub clone/init, active project, PRD write).",
  "type": "module",
  "main": "dist/project-api",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --compile --outfile ./dist/project-api",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
