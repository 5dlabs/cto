<?xml version="1.0" encoding="UTF-8"?>
<task id="4" priority="high" agent="rex">
    <meta>
        <title>Setup Redis connection and caching layer</title>
        <priority>high</priority>
        <dependencies>1</dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Senior Rust Engineer with expertise in systems programming and APIs. Implement Task 4 with production-quality code.</role>

    <context>
        <overview>Configure Redis connection for session management and rate limiting</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Add Redis client configuration in redis.rs module
2. Create connection pool with redis::aio::ConnectionManager
3. Implement session storage interface using Redis with TTL
4. Setup rate limiting storage with Redis counters and sliding window
5. Add Redis configuration from environment (REDIS_URL)</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-4): Setup Redis connection and caching layer</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Integration tests for Redis connectivity and basic set/get operations with TTL verification</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
