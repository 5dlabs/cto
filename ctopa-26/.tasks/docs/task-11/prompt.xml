<?xml version="1.0" encoding="UTF-8"?>
<task id="11" priority="medium" agent="cipher">
    <meta>
        <title>Implement rate limiting middleware</title>
        <priority>medium</priority>
        <dependencies>3, 4</dependencies>
        <agent_hint>cipher</agent_hint>
    </meta>

    <role>You are a Senior Security Engineer with expertise in authentication and secure coding. Implement Task 11 with production-quality code.</role>

    <context>
        <overview>Create rate limiting system using Redis with different limits for authenticated vs anonymous users</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. Create rate limiting middleware using Redis sliding window algorithm
2. Implement different limits: 100 req/min for authenticated, 20 req/min for anonymous
3. Use IP address for anonymous users, user_id for authenticated users
4. Add rate limit headers in responses (X-RateLimit-Limit, X-RateLimit-Remaining)
5. Return 429 status with retry-after header when limit exceeded</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-11): Implement rate limiting middleware</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Test rate limiting for both user types, verify header inclusion, validate limit reset behavior</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
