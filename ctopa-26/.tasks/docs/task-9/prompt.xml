<?xml version="1.0" encoding="UTF-8"?>
<task id="9" priority="high" agent="rex">
    <meta>
        <title>Implement team invitation system</title>
        <priority>high</priority>
        <dependencies>8</dependencies>
        <agent_hint>rex</agent_hint>
    </meta>

    <role>You are a Senior Rust Engineer with expertise in systems programming and APIs. Implement Task 9 with production-quality code.</role>

    <context>
        <overview>Create team invitation links with expiration and acceptance flow</overview>
        <background>Review PRD and architecture in .tasks/docs/ for full context.</background>
    </context>

    <requirements>
        <description>1. POST /api/teams/:id/invite - Generate invite token with 7-day expiration
2. Store invite tokens in Redis with team_id and role information
3. GET /api/invites/:token - Validate and display invite details
4. POST /api/invites/:token/accept - Accept invitation and add user to team
5. DELETE /api/teams/:id/invites/:token - Revoke invitation (admin+ only)</description>
        <constraints>
            <constraint>Match existing codebase patterns</constraint>
            <constraint>Create PR with atomic commits</constraint>
            <constraint>Include unit tests</constraint>
            <constraint>PR title: feat(task-9): Implement team invitation system</constraint>
        </constraints>
    </requirements>

    <acceptance_criteria>
        <criterion>All requirements implemented</criterion>
        <criterion>Tests passing with adequate coverage</criterion>
        <criterion>Code follows project conventions</criterion>
        <criterion>Test invite generation, verify expiration handling, validate acceptance flow and duplicate prevention</criterion>
    </acceptance_criteria>

    <validation>
        <command>cargo test (Rust)</command>
        <command>cargo clippy -- -D warnings (Rust)</command>
        <command>go test ./... (Go)</command>
        <command>npm test (Node.js)</command>
    </validation>

    <deliverables>
        <deliverable>Working implementation</deliverable>
        <deliverable>Unit tests</deliverable>
        <deliverable>Pull request</deliverable>
    </deliverables>
</task>
