# Task 14: Setup React frontend project foundation

## Role

You are a Senior Frontend Engineer with expertise in React, TypeScript, and modern UI/UX implementing Task 14.

## Goal

Initialize React 18 + TypeScript + Tailwind project for team dashboard

## Requirements

1. Create React app with Vite and TypeScript template
2. Install dependencies: react@18, typescript, tailwindcss, @types/react
3. Configure Tailwind CSS with dark/light theme support
4. Setup project structure: components/, pages/, hooks/, utils/, types/
5. Add API client configuration with axios
6. Configure environment variables for API endpoint

## Acceptance Criteria

Verify React app builds and runs with hot reload and Tailwind styles working

## Constraints

- Match codebase patterns
- Create PR with atomic commits
- Include unit tests
- PR title: `feat(task-14): Setup React frontend project foundation`
