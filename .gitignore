# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev-debug.log

# Dependency directories
node_modules/

# Environment variables
.env
.env.*
.env.local
*.env

# Agent secrets (SSH keys and tokens) - at root level only
/agents/
**/id_ed25519
**/id_ed25519.pub
**/.env
# Editor directories and files
.idea
.vscode
.cursor
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS specific
.DS_Store
*.swp
*~

# Kubernetes Files
kubeconfig
kubeconfig-*
*.kubeconfig
# Don't ignore kubeconfig.rs source files
!**/kubeconfig.rs

# Talos cluster configuration (contains secrets)
infra/talos/config/simple/controlplane.yaml
infra/talos/config/simple/worker.yaml
infra/talos/config/simple/worker-*.yaml
infra/talos/config/simple/talosconfig


# Test and Debug Files
test-*.yaml
debug-*.yaml
!infra/gitops/applications/test-db.yaml
!infra/gitops/rbac/test-db-access.yaml

# Helm Files
*.tgz
charts/*.tgz
Chart.lock
**/charts/*.tgz

# Data Files (local persistence)
/data/

# Temporary Files
/tmp/
/temp/
/bin/
tmp.json
tmp-*.json
watch_*

# Backup Files
*.bak
*.backup
*.old
cto-config.json.backup

# Taskmaster directory (local task management)
# .taskmaster/

# Claude configuration files
**/CLAUDE.md

# Rust build artifacts
target/
controller/target/
mcp/target/
sidecar/target/

# Agent files
agent_*.md

# cargo-dist research
.repos/

# Pre-built binaries for Docker builds (CI artifacts)
infra/images/heal/heal-linux
infra/images/tools/tools-server-linux

# Bun-compiled standalone binaries (CI build artifacts)
grok-cron-*

.claude/*

*.private-key.pem

# Local temp working dir for ephemeral clones
.tmp/

# Test repositories
.test-repo/

# Testing template directory - exclude any .git if accidentally created
testing/cto-parallel-test/.git
testing/cto-parallel-test-template/.git

# Solana credentials
sol.env

# Cloudflare credentials
cloudflare.env

# Wrangler local cache (contains account IDs)
**/.wrangler/

# Next.js build cache
.next/

# Test results
test-results/

# Audit reports (local generated files)
*_audit_report.md

# Factory credentials
factory.env

# Vault keys (NEVER commit these!)
vault-keys*.json

# Charts repository (for local development)
charts-repo/
dist/
# Task files
# tasks.json
# tasks/ 

# Secrets backup - never commit
secrets-backup-*/

# Platform packaging docs (private business documents)
docs/platform-packaging/

# Documentation cache (build artifacts)
.docs-cache/

# Cloned reference repos (for research only)
docs/bare-metal/latitude-cli/

# Test project directories (auto-generated by intake/play workflows)
agent-sandbox/
agent-remediation-loop/

# Duplicate templates directory (stale copy - real templates are in templates/)
agent-templates/

# Documentation archive (private/archived docs)
.docs-archive/

# Documentation - all files in docs/ are tracked except specific private directories below

# Local scripts (development utilities, not for version control)
/.scripts/
# (9,992 redundant individual target/ file entries removed - covered by target/ glob above)
.codeql/

# Build logs
.build-*.log

# Ralph monitor runtime files (local state, debugging output)
.ralph-last-branch
**/.ralph-last-branch
**/.ralph-monitor-state.json
**/.ralph-monitor.pid
**/.ralph-events.json
**/.ralph-timing.json
**/ralph-report.html
**/.ralph-failures/
!.claude/skills/

# CLI test secrets
tests/cli-invocation/.env
tests/cli-invocation/.build-info

# Local credentials (private - never commit)
local/
CREDENTIALS.md
credentials.md
*.credentials.*
skills/trader/.env
skills/trader/**/.env
skills/trader/**/.secrets/

# Intake coordinator feedback-loop status (local)
intake/.feedback-loop-state.json
# Pipeline timing epoch (written at pipeline start)
.intake/.pipeline-start-epoch
# Initial task parse output (pre-deliberation)
.intake/initial-tasks.json
# 1Password inject file (see intake/local.env.op.example)
intake/local.env.op
# Local bridge stdout/stderr (run-local-bridges.sh)
intake/.bridge-logs/
# Context files written at pipeline runtime (consumed via jq --rawfile)
.intake/contexts/
# lobster-voice audio cache (ElevenLabs TTS)
.intake/.voice-cache/
.intake/.voice-locks/

# lobster-voice compiled binary (build artifact)
apps/lobster-voice/lobster-voice
