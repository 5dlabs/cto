# Session Memory 2026-02-03

## Webhook Configuration - Stitch Roles
- Stitch now handles `github/pr` webhooks (explicit routing in clawdbot.json)
- Stitch now handles `ci/failed` webhooks (for remediation buttons)
- Both webhooks configured with `agentId: "stitch"` and `wakeMode: "now"`

## Agent Status - Workspace Migration Complete
- All 22 agent worktrees migrated to `/Users/jonathonfritz/agents/<agent-name>`
- 3 agents still on old paths (healer-fix, home, main) - intentionally left
- All agents verified online and responding

## OpenClaw Acknowledgment Gap
- Agents go silent when busy - no built-in acknowledgment for queued messages
- Current `ackReactionScope: "group-mentions"` only handles reactions
- No native "message received" acknowledgment feature found
- Option to add `deliver: true` to webhooks for immediate wake

## Key Files Modified
- `/Users/jonathonfritz/.clawdbot/clawdbot.json` - webhook mappings updated

## Follow-up Tasks
- Consider adding acknowledgment/queue messages to agents
- Optionally enable `deliver: true` for more webhooks
