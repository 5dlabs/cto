# Memory: 2026-02-04

## Completed

### Scaleway Provider Integration

**Goal**: Add native Scaleway support to OpenClaw for bare metal server provisioning

**Accomplished**:
- ✅ Saved Scaleway API credentials to 1Password ("Scaleway API" in Automation vault)
- ✅ Created `.env.scaleway.example` for reference
- ✅ Added Apple Silicon support to existing Rust provider
  - New `apple_silicon.rs` client for Mac mini M2/M2 Pro servers
  - Added Apple Silicon types to `models.rs`
  - Updated `mod.rs` to export both `Scaleway` (Elastic Metal) and `AppleSilicon` clients
- ✅ Code compiles successfully

**Files Modified/Created**:
- `crates/metal/src/providers/scaleway/mod.rs`
- `crates/metal/src/providers/scaleway/models.rs`
- `crates/metal/src/providers/scaleway/apple_silicon.rs` (new)
- `.env.scaleway.example` (new)

**Credentials**:
- Access Key ID: SCWB0JT6WFT6WSPRMG6N
- Secret Key: 226baf25-7824-4e74-8ea3-725dd15d84c8
- Default Zone: fr-par-1

## Provider Capabilities

### Elastic Metal (Bare Metal x86)
- List/create/delete servers
- Install/reinstall OS
- Server actions (reboot, power, rescue mode)
- Flexible IPs

### Apple Silicon (Mac mini M2/M2 Pro)
- List/create/delete servers
- Get remote desktop URL (.novnc)
- Server actions (reboot, reinstall)

## Next Steps

1. Test both providers with real API
2. Add SSH key management methods if needed
3. Integrate with OpenClaw provider registry (if not already)

## Context

- Decided against using official SDK - manual HTTP client is simpler and works
- No Terraform needed - pure API approach for provisioning
- Agents handle SSH/deployment after servers are provisioned
