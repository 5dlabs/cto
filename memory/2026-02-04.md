# 2026-02-04 Session Notes

## CTO Lite Swarm Mode Active

Operating in **Swarm Mode** using Claude Code's TeammateTool for parallel orchestration of CTO Lite implementation.

### Team Structure

| Worker | Focus | Status |
|--------|-------|--------|
| `tauri-dev` | Tauri/Rust backend | Active |
| `react-ui` | React frontend | Active |
| `k8s-infra` | Kind/Helm charts | Active |
| `mcp-tools` | MCP server | Active |

### Swarm Commands

Using `claudesp` binary at `/Users/jonathonfritz/.local/bin/claudesp` for swarm features. Standard `claude` CLI lacks TeammateTool capabilities.

```bash
# Swarm workflow
Teammate({ operation: "spawnTeam", team_name: "cto-lite" })
Task({ team_name: "cto-lite", name: "tauri-dev", subagent_type: "general-purpose", prompt: "...", run_in_background: true })
```

## CTO Lite Implementation Phases

### Phase 1: Tauri App Foundation
- ✅ Set up Tauri project structure (`crates/cto-lite/`)
- ✅ React UI scaffolding with shadcn/ui
- ⚠️ Setup wizard (stack selection, API keys, OAuth) - pending
- ⚠️ Container runtime detection - pending
- ⚠️ Kind cluster management - pending

### Phase 2: Core Infrastructure
- ⏳ Create `cto-lite` Helm chart
- ⏳ Fork PM server to `pm-lite`
- ⏳ Update agent prompts
- ⏳ Tunnel allocation system
- ⏳ Bundle skills into agent images

### Phase 3: Dashboard and MCP
- ⏳ Workflow status/logs view
- ⏳ MCP background service
- ⏳ GitHub App integration
- ⏳ Log streaming

### Phase 4: Distribution
- ⏳ Native installers (macOS/Windows/Linux)
- ⏳ CI for Tauri builds
- ⏳ Code signing setup
- ⏳ CDN for installers

## Key Files Reference

| File | Purpose |
|------|---------|
| `docs/cto-lite.md` | Master implementation plan (800+ lines) |
| `crates/controller/` | Reuse as-is |
| `crates/pm/` | Fork to `pm-lite` |
| `crates/intake/` | Reuse as-is |
| `crates/mcp/` | Fork to MCP-lite |
| `infra/charts/cto/` | Fork to `cto-lite/` |

## Important Constraints

**ALLOWED paths:**
- `crates/cto-lite/` - ALL Lite-specific code
- `infra/charts/cto-lite/` - Lite Helm chart
- `templates/workflows/play-workflow-lite.yaml` - Lite workflow template
- `docs/cto-lite.md` - Plan document

**FORBIDDEN paths:**
- `crates/controller/` - READ ONLY
- `crates/pm/` - READ ONLY (fork instead)
- `crates/mcp/` - READ ONLY (fork instead)
- `crates/healer/` - EXCLUDED (enterprise only)

## Execution Standards

Before claiming done:
- `cargo fmt --all --check`
- `cargo clippy --all-targets -- -D warnings -W clippy::pedantic`
- `cargo test`
- `pre-commit run --all-files`

## Current Session Context

- **Runtime**: agent=conductor
- **Host**: Jonathon's Mac mini
- **Worktree**: `/Users/jonathonfritz/clawd-ctolite` (branch: `ctolite/implementation`)
- **Capabilities**: none (Discord channel)
- **Model**: minimax/MiniMax-M2.1 (default_model)

## Swarm Completion Signal

When all phases complete:
```
<swarm>COMPLETE</swarm>
```
