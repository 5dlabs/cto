### Step 1: Create GitHub Issue

```bash
ISSUE_URL=$(gh issue create \
  --repo 5dlabs/cto \
  --title "[HEAL-{{alert_id}}] {{issue_title}}" \
  --label "heal,remediation,{{lowercase alert_id}}" \
  --body "🔍 {{issue_placeholder}}... Full analysis to follow.")
if [ -z "$ISSUE_URL" ]; then
  echo "❌ Failed to create GitHub issue"
  exit 1
fi
ISSUE_NUMBER=$(echo "$ISSUE_URL" | grep -oE '[0-9]+$')
if [ -z "$ISSUE_NUMBER" ]; then
  echo "❌ Failed to extract issue number from: $ISSUE_URL"
  exit 1
fi
echo "✅ Created issue #${ISSUE_NUMBER}"
```

